# This package must be installed with npm, not pnpm
# The workspace=false setting tells npm to ignore the parent workspace
# and install all dependencies locally

# Prevent npm from using the workspace
workspaces=false

# Use hoisted structure for compatibility
node-linker=hoisted
