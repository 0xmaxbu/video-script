/**
 * Remotion configuration
 *
 * Note: This file is kept for Remotion Studio (local preview) only.
 * Production rendering uses the Playwright-based frame renderer
 * (puppeteer-renderer.ts) which bypasses the Remotion bundler entirely.
 */
