---
phase: 06-type-schema
plan: "06-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/types/src/visual.ts
  - packages/types/src/script.ts
  - packages/types/src/shared.ts
  - packages/types/src/research.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - RES-01
  - RES-03
  - SCR-01
  - SCR-02
  - COMP-01
must_haves:
  truths:
    - "@video-script/types package exists and exports all shared types"
    - "Annotation type includes type, target, style, narrationBinding fields per VIS-01/02/03"
    - "SceneScriptSchema includes highlights and codeHighlights fields per D-02"
    - "ScreenshotConfigBaseSchema includes background, width, fontSize, fontFamily per D-03"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "Single-entry exports for all shared types and schemas"
      exports:
        - "AnnotationSchema, Annotation, AnnotationTypeEnum, AnnotationColorEnum"
        - "SceneScriptSchema, ScriptOutputSchema, SceneNarrativeType"
        - "SceneHighlightSchema, CodeHighlightSchema"
        - "ScreenshotConfigBaseSchema"
        - "ResearchOutputSchema, ResearchSegmentSchema"
    - path: "packages/types/src/visual.ts"
      provides: "Annotation system types from src/types/visual.ts"
      contains: "AnnotationSchema, AnnotationTypeEnum, AnnotationColorEnum, AnnotationTargetSchema"
    - path: "packages/types/src/script.ts"
      provides: "Script output schemas with highlights/codeHighlights"
      contains: "SceneScriptSchema with highlights: z.array(SceneHighlightSchema).optional(), codeHighlights: z.array(CodeHighlightSchema).optional()"
    - path: "packages/types/src/shared.ts"
      provides: "Base ScreenshotConfig and shared types"
      contains: "ScreenshotConfigBaseSchema with background, width, fontSize, fontFamily"
---

<objective>
Create the `@video-script/types` shared package that exports unified TypeScript types and zod schemas for use by both the main process (zod v4) and the renderer subprocess (zod v3).
</objective>

<context>
@src/types/visual.ts
@src/types/script.ts
@packages/renderer/src/remotion/Scene.tsx
@packages/renderer/src/remotion/annotations/AnnotationRenderer.tsx

# Current State
- `packages/types/` directory does not exist
- Renderer imports `import type { Annotation } from "@video-script/types"` which does not exist
- Annotation types are defined in `src/types/visual.ts` but not exposed as a shareable package

# Phase Requirements (VIS-01, VIS-02, VIS-03, RES-01, RES-03, SCR-01, SCR-02, COMP-01)
All requirements need the unified `@video-script/types` package to resolve the schema fragmentation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/types package.json</name>
  <files>packages/types/package.json</files>
  <action>
Create `packages/types/package.json` with:
```json
{
  "name": "@video-script/types",
  "version": "0.1.0",
  "description": "Shared TypeScript types and zod schemas for video-script",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "zod": "^4.3.6"
  },
  "files": ["dist"]
}
```

Key decisions per D-01:
- zod in devDependencies only (consumers bring their own zod)
- No production dependencies (leaf package)
- conditional exports for clean ESM/CJS split
</action>
  <verify>
grep -q '"name": "@video-script/types"' packages/types/package.json
grep -q '"devDependencies"' packages/types/package.json
grep -q '"zod"' packages/types/package.json
</verify>
  <done>packages/types/package.json created with correct name and devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create packages/types/tsconfig.json</name>
  <files>packages/types/tsconfig.json</files>
  <action>
Create `packages/types/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "noImplicitAny": true,
    "exactOptionalPropertyTypes": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```
</action>
  <verify>
grep -q '"outDir": "./dist"' packages/types/tsconfig.json
grep -q '"declaration": true' packages/types/tsconfig.json
</verify>
  <done>packages/types/tsconfig.json created with strict mode and declaration output</done>
</task>

<task type="auto">
  <name>Task 3: Create packages/types/src/index.ts</name>
  <files>packages/types/src/index.ts</files>
  <action>
Create `packages/types/src/index.ts` that re-exports all types and schemas:

```typescript
// Re-export all shared types from submodules
export * from "./visual.js";
export * from "./script.js";
export * from "./shared.js";
export * from "./research.js";
```

This is the single-entry point. Consumers import from "@video-script/types" which resolves to this file.
</action>
  <verify>
grep -q 'export * from "./visual.js"' packages/types/src/index.ts
grep -q 'export * from "./script.js"' packages/types/src/index.ts
grep -q 'export * from "./shared.js"' packages/types/src/index.ts
</verify>
  <done>packages/types/src/index.ts re-exports from all submodules</done>
</task>

<task type="auto">
  <name>Task 4: Create packages/types/src/visual.ts (Annotation types)</name>
  <files>packages/types/src/visual.ts</files>
  <action>
Create `packages/types/src/visual.ts` with the Annotation system types extracted from `src/types/visual.ts`:

```typescript
import { z } from "zod";

// Annotation types
export const AnnotationTypeEnum = z.enum([
  "circle",
  "underline",
  "arrow",
  "box",
  "highlight",
  "number",
  "crossout",
  "checkmark",
]);
export type AnnotationType = z.infer<typeof AnnotationTypeEnum>;

export const AnnotationColorEnum = z.enum([
  "attention",
  "highlight",
  "info",
  "success",
]);
export type AnnotationColor = z.infer<typeof AnnotationColorEnum>;

export const AnnotationTargetSchema = z.object({
  type: z.enum(["text", "region", "code-line"]),
  textMatch: z.string().optional(),
  lineNumber: z.number().int().positive().optional(),
  region: z
    .enum(["top-left", "top-right", "center", "bottom-left", "bottom-right"])
    .optional(),
});
export type AnnotationTarget = z.infer<typeof AnnotationTargetSchema>;

export const AnnotationSchema = z.object({
  type: AnnotationTypeEnum,
  target: AnnotationTargetSchema,
  style: z.object({
    color: AnnotationColorEnum,
    size: z.enum(["small", "medium", "large"]),
  }),
  narrationBinding: z.object({
    triggerText: z.string(),
    segmentIndex: z.number().int().nonnegative(),
    appearAt: z.number().nonnegative(),
  }),
});
export type Annotation = z.infer<typeof AnnotationSchema>;

// SceneHighlight type (for script highlights)
export const SceneHighlightSchema = z.object({
  text: z.string(),
  segmentIndex: z.number().int().nonnegative(),
  charStart: z.number().int().nonnegative(),
  charEnd: z.number().int().nonnegative(),
  timeInScene: z.number().nonnegative(),
  importance: z.enum(["critical", "high", "medium"]),
  annotationSuggestion: z.enum([
    "circle",
    "underline",
    "highlight",
    "number",
  ]),
  reason: z.string(),
});
export type SceneHighlight = z.infer<typeof SceneHighlightSchema>;

// CodeHighlight type (for code annotations)
export const CodeHighlightSchema = z.object({
  codeLine: z.number().int().positive(),
  codeText: z.string(),
  explanation: z.string(),
  timeInScene: z.number().nonnegative(),
  annotationType: z.enum(["circle", "underline", "arrow", "number"]),
});
export type CodeHighlight = z.infer<typeof CodeHighlightSchema>;

// Annotation colors runtime values
export const ANNOTATION_COLORS = {
  attention: "#FF3B30",
  highlight: "#FFCC00",
  info: "#007AFF",
  success: "#34C759",
} as const;
export type AnnotationColorValue =
  (typeof ANNOTATION_COLORS)[AnnotationColor];
```

This matches exactly the annotation types that `AnnotationRenderer.tsx` and `Scene.tsx` expect (per their imports from "@video-script/types").
</action>
  <verify>
grep -q 'AnnotationTypeEnum' packages/types/src/visual.ts
grep -q 'AnnotationSchema' packages/types/src/visual.ts
grep -q 'SceneHighlightSchema' packages/types/src/visual.ts
grep -q 'CodeHighlightSchema' packages/types/src/visual.ts
grep -q 'ANNOTATION_COLORS' packages/types/src/visual.ts
</verify>
  <done>packages/types/src/visual.ts exports Annotation types matching renderer expectations</done>
</task>

<task type="auto">
  <name>Task 5: Create packages/types/src/script.ts (SceneScriptSchema with highlights)</name>
  <files>packages/types/src/script.ts</files>
  <action>
Create `packages/types/src/script.ts` with unified SceneScriptSchema that includes highlights and codeHighlights per D-02:

```typescript
import { z } from "zod";
import {
  AnnotationSchema,
  SceneHighlightSchema,
  CodeHighlightSchema,
} from "./visual.js";
import {
  VisualTypeEnum,
  PositionSchema,
  AnimationConfigSchema,
} from "./shared.js";

export const SceneNarrativeType = z.enum(["intro", "feature", "code", "outro"]);
export type SceneNarrativeType = z.infer<typeof SceneNarrativeType>;

export const VisualLayerSchema = z.object({
  id: z.string(),
  type: VisualTypeEnum,
  position: PositionSchema,
  content: z.string(),
  animation: AnimationConfigSchema,
});
export type VisualLayer = z.infer<typeof VisualLayerSchema>;

export const SceneTransitionSchema = z.object({
  type: z.enum(["fade", "slide", "wipe", "flip", "clockWipe", "iris", "none"]),
  duration: z.number().min(0).max(1),
});
export type SceneTransition = z.infer<typeof SceneTransitionSchema>;

// D-02: Unified SceneScriptSchema with highlights and codeHighlights
export const SceneScriptSchema = z.object({
  id: z.string(),
  type: SceneNarrativeType,
  title: z.string(),
  narration: z.string(),
  duration: z.number().positive(),
  visualLayers: z.array(VisualLayerSchema).optional(),
  transition: SceneTransitionSchema.optional(),
  // D-02a: highlights from script agent - marks narration highlights with timing
  highlights: z.array(SceneHighlightSchema).optional(),
  // D-02a: codeHighlights - marks code line annotations with timing
  codeHighlights: z.array(CodeHighlightSchema).optional(),
  sourceRef: z.string().optional(),
});
export type SceneScript = z.infer<typeof SceneScriptSchema>;

export const ScriptOutputSchema = z
  .object({
    title: z.string(),
    totalDuration: z.number().positive(),
    scenes: z.array(SceneScriptSchema).min(1),
  })
  .strict();
export type ScriptOutput = z.infer<typeof ScriptOutputSchema>;
```

Key per D-02: Both `highlights` and `codeHighlights` are optional fields on `SceneScriptSchema`.
</action>
  <verify>
grep -q 'SceneScriptSchema' packages/types/src/script.ts
grep -q 'highlights: z.array(SceneHighlightSchema).optional()' packages/types/src/script.ts
grep -q 'codeHighlights: z.array(CodeHighlightSchema).optional()' packages/types/src/script.ts
grep -q 'ScriptOutputSchema' packages/types/src/script.ts
</verify>
  <done>packages/types/src/script.ts exports unified SceneScriptSchema with highlights and codeHighlights</done>
</task>

<task type="auto">
  <name>Task 6: Create packages/types/src/shared.ts (Base types)</name>
  <files>packages/types/src/shared.ts</files>
  <action>
Create `packages/types/src/shared.ts` with shared base types per D-03:

```typescript
import { z } from "zod";

// Visual type enum
export const VisualTypeEnum = z.enum([
  "screenshot",
  "code",
  "text",
  "diagram",
  "image",
]);
export type VisualType = z.infer<typeof VisualTypeEnum>;

// Position schema
export const PositionSchema = z.object({
  x: z.union([z.number().min(0), z.enum(["left", "center", "right"])]),
  y: z.union([z.number().min(0), z.enum(["top", "center", "bottom"])]),
  width: z.union([z.number().min(0), z.literal("auto"), z.literal("full")]),
  height: z.union([z.number().min(0), z.literal("auto"), z.literal("full")]),
  zIndex: z.number().default(0),
});

// Animation config
export const AnimationConfigSchema = z.object({
  enter: z.enum([
    "fadeIn",
    "slideLeft",
    "slideRight",
    "slideUp",
    "slideDown",
    "slideIn",
    "zoomIn",
    "typewriter",
    "none",
  ]),
  enterDelay: z.number().default(0),
  exit: z.enum(["fadeOut", "slideOut", "zoomOut", "none"]),
  exitAt: z.number().optional(),
});

// D-03: Base ScreenshotConfigSchema (common fields)
// Renderer-specific fields (maxLines, padding, theme) extend in packages/renderer/src/types.ts
export const ScreenshotConfigBaseSchema = z.object({
  background: z.string().default("#1E1E1E"),
  width: z.number().int().positive().default(1920),
  fontSize: z.number().int().positive().default(14),
  fontFamily: z.string().default("Fira Code"),
});
export type ScreenshotConfigBase = z.infer<typeof ScreenshotConfigBaseSchema>;
```

Key per D-03: Base schema has common fields only. Renderer extends with maxLines, padding, theme.
</action>
  <verify>
grep -q 'ScreenshotConfigBaseSchema' packages/types/src/shared.ts
grep -q 'background: z.string().default("#1E1E1E")' packages/types/src/shared.ts
grep -q 'PositionSchema' packages/types/src/shared.ts
grep -q 'AnimationConfigSchema' packages/types/src/shared.ts
</verify>
  <done>packages/types/src/shared.ts exports base ScreenshotConfig and shared types</done>
</task>

<task type="auto">
  <name>Task 7: Create packages/types/src/research.ts (Research types)</name>
  <files>packages/types/src/research.ts</files>
  <action>
Create `packages/types/src/research.ts` with research schemas from `src/types/script.ts`:

```typescript
import { z } from "zod";

// Research segment schema per RES-03
export const ResearchSegmentSchema = z.object({
  order: z.number().int().positive(),
  sentence: z.string(),
  keyContent: z.object({
    concept: z.string(),
  }),
  links: z.array(
    z.object({
      url: z.string(),
      key: z.string(),
    }),
  ),
});
export type ResearchSegment = z.infer<typeof ResearchSegmentSchema>;

// Research output schema per RES-01
export const ResearchOutputSchema = z.object({
  title: z.string(),
  segments: z.array(ResearchSegmentSchema),
});
export type ResearchOutput = z.infer<typeof ResearchOutputSchema>;
```

These match the research schemas already in `src/types/script.ts` for RES-01 and RES-03.
</action>
  <verify>
grep -q 'ResearchOutputSchema' packages/types/src/research.ts
grep -q 'ResearchSegmentSchema' packages/types/src/research.ts
</verify>
  <done>packages/types/src/research.ts exports research schemas for RES-01 and RES-03</done>
</task>

<task type="auto">
  <name>Task 8: Build packages/types</name>
  <files>packages/types/dist/index.js</files>
  <action>
Build the types package:

```bash
cd packages/types && npm install
npm run build --workspace=packages/types
```

This compiles TypeScript and generates dist/ with .js and .d.ts files.

Note: We use `npm run build --workspace=packages/types` from root since this is a workspace package.
</action>
  <verify>
ls packages/types/dist/index.js
ls packages/types/dist/index.d.ts
</verify>
  <done>packages/types compiled with dist/index.js and dist/index.d.ts generated</done>
</task>

</tasks>

<verification>
After plan 06-01 completes:
1. `packages/types/dist/index.js` exists
2. `packages/types/dist/index.d.ts` exists
3. `@video-script/types` can be imported by TypeScript
4. All required types are exported: AnnotationSchema, SceneScriptSchema, ScriptOutputSchema, ScreenshotConfigBaseSchema, ResearchOutputSchema
</verification>

<success_criteria>
- packages/types package.json has correct name "@video-script/types"
- All source files created in packages/types/src/
- TypeScript compiles without errors
- dist/ contains compiled .js and .d.ts files
- Exports include Annotation types (VIS-01/02/03), SceneScriptSchema with highlights/codeHighlights (SCR-01/02), ResearchOutputSchema (RES-01/03)
</success_criteria>

<output>
After completion, create `.planning/phases/06-type-schema/06-01-SUMMARY.md`
</output>
