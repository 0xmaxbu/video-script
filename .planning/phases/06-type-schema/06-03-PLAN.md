---
phase: 06-type-schema
plan: "06-03"
type: checkpoint:human-verify
wave: 3
depends_on:
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - RES-01
  - RES-03
  - SCR-01
  - SCR-02
  - COMP-01
must_haves:
  truths:
    - "TypeScript compiles without errors"
    - "@video-script/types exports all required types"
    - "Schema unification verified - highlights and codeHighlights flow correctly"
  artifacts:
    - path: "packages/types/dist/"
      provides: "Compiled @video-script/types package"
    - path: "packages/renderer/src/types.ts"
      provides: "Renderer uses unified schemas"
---

<objective>
Verify the type package creation and schema unification works end-to-end. This is a human verification checkpoint.
</objective>

<context>
@packages/types/src/index.ts
@packages/renderer/src/types.ts
@src/types/index.ts

# What was built
- Plan 06-01: Created @video-script/types package with unified schemas
- Plan 06-02: Updated consumers to use @video-script/types

# What needs verification
1. TypeScript compilation succeeds
2. All required types are exported and usable
3. Schema unification is correct
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Verify TypeScript compilation</name>
  <files>packages/types/dist/index.d.ts</files>
  <action>
Run TypeScript typecheck to verify everything compiles:

```bash
npm run typecheck
```

Expected: No TypeScript errors.

If errors occur, they typically relate to:
- Import path resolution for @video-script/types
- Schema compatibility issues
- Missing exports

Fix any issues before proceeding.
</action>
  <verify>
npm run typecheck
</verify>
  <done>TypeScript compilation succeeds with no errors</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify @video-script/types exports</name>
  <files>packages/types/dist/index.d.ts</files>
  <action>
Verify the @video-script/types package exports all required types by checking dist/index.d.ts:

```bash
cat packages/types/dist/index.d.ts | head -100
```

Expected exports:
- Annotation, AnnotationSchema, AnnotationTypeEnum, AnnotationColorEnum
- SceneScript, SceneScriptSchema, ScriptOutput, ScriptOutputSchema
- SceneHighlight, SceneHighlightSchema, CodeHighlight, CodeHighlightSchema
- ScreenshotConfigBase, ScreenshotConfigBaseSchema
- ResearchOutput, ResearchOutputSchema, ResearchSegment, ResearchSegmentSchema
- VisualLayer, VisualLayerSchema, SceneTransition, SceneTransitionSchema
- Position, PositionSchema, AnimationConfig, AnimationConfigSchema
</action>
  <verify>
grep -c 'AnnotationSchema' packages/types/dist/index.d.ts
grep -c 'SceneScriptSchema' packages/types/dist/index.d.ts
grep -c 'SceneHighlightSchema' packages/types/dist/index.d.ts
grep -c 'CodeHighlightSchema' packages/types/dist/index.d.ts
grep -c 'ResearchOutputSchema' packages/types/dist/index.d.ts
</verify>
  <done>All required types are exported from @video-script/types</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify schema unification</name>
  <files>packages/renderer/src/types.ts</files>
  <action>
Verify that packages/renderer/src/types.ts correctly imports from @video-script/types and that SceneScriptSchema includes highlights and codeHighlights:

```bash
grep -A5 'SceneScriptSchema' packages/renderer/src/types.ts
```

Expected: Should see `export { SceneScriptSchema } from "@video-script/types"` or similar import.

Also verify that the renderer ScreenshotConfig extends the base:
```bash
grep -A3 'ScreenshotConfigBaseSchema' packages/renderer/src/types.ts
```

Expected: Should show ScreenshotConfigSchema = ScreenshotConfigBaseSchema.extend({...})
</action>
  <verify>
grep -c 'from "@video-script/types"' packages/renderer/src/types.ts
grep -c 'ScreenshotConfigBaseSchema' packages/renderer/src/types.ts
</verify>
  <done>Schema unification verified - renderer imports from @video-script/types</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify remotion-project-generator.ts fixes</name>
  <files>packages/renderer/src/remotion-project-generator.ts</files>
  <action>
Verify that remotion-project-generator.ts no longer uses z.any() for critical fields:

```bash
grep -n 'z\.any()' packages/renderer/src/remotion-project-generator.ts
```

Expected: No z.any() for transition or annotations. May have z.any() elsewhere (check context).

If z.any() remains for transition/annotations, those are the fields that need fixing per D-02c.
</action>
  <verify>
# Count z.any() occurrences - should be minimal or zero
grep -c 'z\.any()' packages/renderer/src/remotion-project-generator.ts
# Verify proper schema imports exist
grep -c 'from "@video-script/types"' packages/renderer/src/remotion-project-generator.ts
</verify>
  <done>remotion-project-generator.ts uses proper schemas instead of z.any()</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Run `npm run typecheck` - should pass
2. Check packages/types/dist/index.d.ts contains all expected exports
3. Verify renderer types.ts imports from @video-script/types
4. Verify no z.any() remains for transition/annotations in remotion-project-generator.ts
</verification>

<success_criteria>
- npm run typecheck passes
- @video-script/types exports Annotation, SceneScript (with highlights/codeHighlights), ResearchOutput
- Renderer imports from @video-script/types
- remotion-project-generator.ts has no z.any() for transition/annotations
- All 8 requirements (VIS-01, VIS-02, VIS-03, RES-01, RES-03, SCR-01, SCR-02, COMP-01) have unified types
</success_criteria>

<output>
After completion, create `.planning/phases/06-type-schema/06-03-SUMMARY.md`
</output>
