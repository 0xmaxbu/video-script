---
phase: 18-14-gap-03-bash
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/manual/phase-18/phase-14-animation-input.md
  - tests/manual/phase-18/review-template.md
  - tests/manual/phase-18/preflight-check.sh
  - tests/manual/phase-18/package-artifacts.sh
autonomous: true
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
user_setup:
  - service: configured-llm-provider
    why: "真实 create / resume 运行会调用 research、script 与质量评估 agent"
    env_vars:
      - name: OPENAI_API_KEY
        source: ".env 或 shell 环境；当 video-script.config.json 的 llm.provider=openai 时必须存在"
      - name: ANTHROPIC_API_KEY
        source: ".env 或 shell 环境；当 video-script.config.json 的 llm.provider=anthropic 时必须存在"
    dashboard_config:
      - task: "确认 video-script.config.json 的 llm.provider 与已配置的 API key 匹配"
        location: "仓库根 video-script.config.json"
must_haves:
  truths:
    - "执行者拥有固定不变的 Phase 18 题材、链接、输出目录和审核口径"
    - "two-run 验证在开始前就能自动检查 build、typecheck、配置与 LLM key 是否齐备"
    - "每次真实运行后都能把 slugged MP4 标准化为 `output.mp4` 并生成工件清单"
  artifacts:
    - path: "tests/manual/phase-18/phase-14-animation-input.md"
      provides: "固定标题、固定 GitHub 链接、固定输出目录、固定 CLI 命令模板"
    - path: "tests/manual/phase-18/review-template.md"
      provides: "动画优先的人工审核模板与三级结论格式"
    - path: "tests/manual/phase-18/preflight-check.sh"
      provides: "build/typecheck/config/env/output-dir 自动预检"
    - path: "tests/manual/phase-18/package-artifacts.sh"
      provides: "标准化 output.mp4、检查完整工件包、输出工件清单"
  key_links:
    - from: "tests/manual/phase-18/preflight-check.sh"
      to: "video-script.config.json"
      via: "读取 llm.provider 并校验对应 env key"
      pattern: "video-script\.config\.json|OPENAI_API_KEY|ANTHROPIC_API_KEY"
    - from: "tests/manual/phase-18/package-artifacts.sh"
      to: "test-output/phase-18/*"
      via: "校验 research/script/screenshots/report/srt/video 并复制 output.mp4"
      pattern: "research\.json|script\.json|quality-report\.md|output\.srt|output\.mp4"
---

<objective>
锁定 Phase 18 的 deterministic 输入、审核模板与 bash 自动化脚本，让后续真实 CLI 运行不需要临场决定主题、链接、目录或工件检查规则。

Purpose: 先把上下文、脚本和审核口径固化，避免执行阶段因为命令拼装或文件名差异导致结果不可复现。
Output: 固定输入文档、人工审核模板、预检脚本、工件打包脚本。
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-14-gap-03-bash/18-CONTEXT.md
@.planning/phases/18-14-gap-03-bash/18-RESEARCH.md
@.planning/phases/14-animation-engine/14-CONTEXT.md
@.planning/phases/14-animation-engine/14-GAP-06-SUMMARY.md
@.planning/phases/17-e2e-testing/17-SUMMARY.md
@src/cli/index.ts
@src/utils/config.ts
</context>

<interfaces>
From `src/cli/index.ts`:

```typescript
program
  .command("create [title]")
  .option("--links <urls>")
  .option("--doc <file>")
  .option("--output <dir>")
  .option("--no-review")
  .option("--aspect-ratio <ratio>", "16:9");

program.command("resume [runId]");
```

From `src/utils/config.ts`:

```typescript
const LlmConfigSchema = z.object({
  provider: z.enum(["openai", "anthropic"]).default("openai"),
  model: z.string().default("gpt-4-turbo"),
  apiKey: z.string().optional(),
});
```

Critical execution constraint from `src/cli/index.ts`:

```typescript
// resume reads persisted workflow state; it does not accept an output dir
const state = workflowStateManager.load();
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 固化 Phase 18 输入集与人工审核模板</name>
  <files>tests/manual/phase-18/phase-14-animation-input.md, tests/manual/phase-18/review-template.md</files>
  <read_first>.planning/phases/18-14-gap-03-bash/18-CONTEXT.md, .planning/phases/18-14-gap-03-bash/18-RESEARCH.md, .planning/phases/14-animation-engine/14-CONTEXT.md, .planning/phases/14-animation-engine/14-GAP-06-SUMMARY.md, src/cli/index.ts</read_first>
  <action>创建 `tests/manual/phase-18/phase-14-animation-input.md`，把以下内容逐字写死，不要只写“参考 Phase 14 文档”：标题固定为 `Phase 14 Animation Engine`；真实链接固定为 5 条 GitHub blob 链接：`https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/.planning/phases/14-animation-engine/14-CONTEXT.md`、`https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/packages/renderer/src/remotion/Composition.tsx`、`https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/packages/renderer/src/remotion/components/ScreenshotLayer.tsx`、`https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/packages/renderer/src/remotion/components/KineticSubtitle.tsx`、`https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/.planning/phases/14-animation-engine/14-GAP-06-SUMMARY.md`；输出目录固定为 `test-output/phase-18/one-shot` 与 `test-output/phase-18/resume`；命令模板必须包含 `node dist/cli/index.js create ... --no-review`、`node dist/cli/index.js create ...`、`node dist/cli/index.js resume`。同时创建 `tests/manual/phase-18/review-template.md`，逐项列出 D-05 / D-06 要求：`转场`、`节奏`、`Ken Burns`、`字幕动效是否出现在最终视频里`、`质量报告是否与成片一致`，并把结论字段写成且只写成 `通过`、`可接受但需优化`、`不通过` 三个选项。</action>
  <acceptance_criteria>
    - `tests/manual/phase-18/phase-14-animation-input.md` contains the exact title `Phase 14 Animation Engine`
    - `tests/manual/phase-18/phase-14-animation-input.md` contains all 5 pinned GitHub blob URLs above
    - `tests/manual/phase-18/phase-14-animation-input.md` contains both `test-output/phase-18/one-shot` and `test-output/phase-18/resume`
    - `tests/manual/phase-18/review-template.md` contains the exact verdict strings `通过`, `可接受但需优化`, `不通过`
    - `tests/manual/phase-18/review-template.md` contains checklist items for `转场`, `节奏`, `Ken Burns`, and `字幕动效`
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -F "Phase 14 Animation Engine" tests/manual/phase-18/phase-14-animation-input.md >/dev/null; rg -F "test-output/phase-18/one-shot" tests/manual/phase-18/phase-14-animation-input.md >/dev/null; rg -F "https://github.com/0xmaxbu/video-script/blob/48a82add83169057c9e04c93b343f775a580842f/packages/renderer/src/remotion/components/KineticSubtitle.tsx" tests/manual/phase-18/phase-14-animation-input.md >/dev/null; rg -F "可接受但需优化" tests/manual/phase-18/review-template.md >/dev/null; rg -F "Ken Burns" tests/manual/phase-18/review-template.md >/dev/null'</automated>
  </verify>
  <done>后续执行者不需要自己挑题、找链接、决定输出目录或发明审核标准。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Phase 18 预检与工件打包脚本</name>
  <files>tests/manual/phase-18/preflight-check.sh, tests/manual/phase-18/package-artifacts.sh</files>
  <read_first>tests/manual/phase-18/phase-14-animation-input.md, src/cli/index.ts, src/utils/config.ts, .planning/phases/17-e2e-testing/17-SUMMARY.md, packages/renderer/src/puppeteer-renderer.ts</read_first>
  <action>创建 `tests/manual/phase-18/preflight-check.sh`，脚本必须按固定顺序执行并在任一步失败时退出：`npm run build`、`npm run typecheck`、确认 `video-script.config.json` 存在、读取其中的 `llm.provider` 值、当 provider 为 `openai` 时要求 `OPENAI_API_KEY` 非空、当 provider 为 `anthropic` 时要求 `ANTHROPIC_API_KEY` 非空、创建并清空 `test-output/phase-18/one-shot` 与 `test-output/phase-18/resume`、最后执行 `node dist/cli/index.js --help >/dev/null`。同时创建 `tests/manual/phase-18/package-artifacts.sh`，接受两个参数：`<output_dir>` 与 `<rendered_mp4_name>`；脚本必须验证 `<output_dir>/research.json`、`script.json`、`quality-report.md`、`output.srt`、`screenshots/` 存在，把 `<output_dir>/<rendered_mp4_name>` 复制为 `<output_dir>/output.mp4`，然后输出 `<output_dir>/artifact-manifest.txt`，其中至少记录 `output.mp4`、`output.srt`、`research.json`、`script.json`、`quality-report.md` 的文件大小与 `screenshots` 中 PNG 数量；如本机有 `ffprobe`，额外记录 `output.mp4` 的 duration 与 resolution。不要在脚本里调用内部 TypeScript helper；只能用 bash、node CLI、`cp`、`wc`、`find`/`rg`、`ffprobe` 这类 shell 工具。</action>
  <acceptance_criteria>
    - `tests/manual/phase-18/preflight-check.sh` contains `npm run build`
    - `tests/manual/phase-18/preflight-check.sh` contains `npm run typecheck`
    - `tests/manual/phase-18/preflight-check.sh` contains checks for both `OPENAI_API_KEY` and `ANTHROPIC_API_KEY`
    - `tests/manual/phase-18/preflight-check.sh` contains both output directories under `test-output/phase-18/`
    - `tests/manual/phase-18/package-artifacts.sh` contains `output.mp4`, `artifact-manifest.txt`, `quality-report.md`, and `output.srt`
    - `tests/manual/phase-18/package-artifacts.sh` copies the rendered MP4 into `<output_dir>/output.mp4`
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; rg -F "npm run build" tests/manual/phase-18/preflight-check.sh >/dev/null; rg -F "OPENAI_API_KEY" tests/manual/phase-18/preflight-check.sh >/dev/null; rg -F "ANTHROPIC_API_KEY" tests/manual/phase-18/preflight-check.sh >/dev/null; rg -F "test-output/phase-18/resume" tests/manual/phase-18/preflight-check.sh >/dev/null; rg -F "output.mp4" tests/manual/phase-18/package-artifacts.sh >/dev/null; rg -F "artifact-manifest.txt" tests/manual/phase-18/package-artifacts.sh >/dev/null'</automated>
  </verify>
  <done>后续运行前能一键预检环境，运行后能一键生成标准化审核工件包。</done>
</task>

</tasks>

<verification>
运行 `bash tests/manual/phase-18/preflight-check.sh` 应在本机配置完整时直接通过；若缺少 API key 或 build/typecheck 失败，应立即中止后续 Phase 18 真跑。</verification>

<success_criteria>

1. Phase 18 的输入、输出目录、命令模板、审核口径全部写死在仓库内文件中。
2. 预检脚本能自动阻止缺配置、缺构建产物、缺输出目录清理的无效运行。
3. 工件打包脚本能把真实运行结果标准化为 `output.mp4` + `artifact-manifest.txt`，供后续计划复用。
   </success_criteria>

<output>
After completion, create `.planning/phases/18-14-gap-03-bash/18-01-SUMMARY.md`
</output>
