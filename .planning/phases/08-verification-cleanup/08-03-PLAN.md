---
phase: 08-verification-cleanup
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/renderer/src/index.ts
  - packages/renderer/src/verification/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "cli.ts imports continue to work after dead export removal"
    - "Generated Remotion projects continue to receive needed types (SceneScriptSchema, ScriptOutputSchema)"
    - "renderVideo function still exports correctly"
  artifacts:
    - path: "packages/renderer/src/index.ts"
      provides: "Clean exports - only used symbols exported"
    - path: "packages/renderer/src/verification/index.ts"
      provides: "Orphaned verification utilities removed"
  key_links:
    - from: "cli.ts"
      to: "index.ts"
      via: "import renderVideo, RenderVideoInputSchema"
      pattern: "import.*renderVideo.*RenderVideoInputSchema"
    - from: "video-renderer.ts"
      to: "index.ts"
      via: "export"
      pattern: "export.*renderVideo"
---

<objective>
Remove dead exports from packages/renderer/src/index.ts and remove orphaned verification utilities. Based on dead export audit in 08-RESEARCH.md, removes 12 unused exports while preserving 8 that are used internally or by generated projects.
</objective>

<context>
@packages/renderer/src/index.ts
@packages/renderer/src/verification/index.ts
@packages/renderer/src/cli.ts
@.planning/phases/08-verification-cleanup/08-RESEARCH.md
</context>

<interfaces>
cli.ts imports (the only internal consumer):
  import { renderVideo, RenderVideoInputSchema } from "./index.js";
  import { generateSrt } from "./srt-generator.js";

Dead exports to REMOVE (not imported by cli.ts):
  - RenderVideoOutputSchema, RenderVideoInput, RenderVideoOutput (from video-renderer.ts)
  - renderVideoWithPuppeteer, PuppeteerRenderInputSchema, PuppeteerRenderOutputSchema, PuppeteerRenderInput, PuppeteerRenderOutput (from puppeteer-renderer.ts)
  - GenerateSrtInputSchema, GenerateSrtOutputSchema, GenerateSrtInput, GenerateSrtOutput (from srt-generator.ts)

Keep exports (used internally or by generated projects):
  - renderVideo, calculateTotalDuration, RenderVideoInputSchema (used by cli.ts)
  - SceneScriptSchema, ScriptOutputSchema, SceneScript, ScriptOutput (used by generated Remotion projects)
  - generateRemotionProject, GenerateProjectInput, GenerateProjectOutput (used internally by renderVideo)
  - cleanupRemotionTempDir (used internally)
  - generateSrt (used by cli.ts)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead exports from index.ts</name>
  <files>packages/renderer/src/index.ts</files>
  <read_first>
    @packages/renderer/src/index.ts
    @packages/renderer/src/cli.ts
  </read_first>
  <action>
Edit packages/renderer/src/index.ts to remove dead exports.

**KEEP these exports:**
  - renderVideo, calculateTotalDuration, RenderVideoInputSchema from video-renderer.js
  - RenderVideoInput type from video-renderer.js
  - SceneScriptSchema, ScriptOutputSchema from types.js
  - SceneScript, ScriptOutput types from types.js
  - generateRemotionProject from remotion-project-generator.js
  - GenerateProjectInput, GenerateProjectOutput types from remotion-project-generator.js
  - cleanupRemotionTempDir from cleanup.js
  - generateSrt from srt-generator.js

**REMOVE these dead exports:**
  - RenderVideoOutputSchema and RenderVideoOutput type from video-renderer.js
  - renderVideoWithPuppeteer, PuppeteerRenderInputSchema, PuppeteerRenderOutputSchema, PuppeteerRenderInput type, PuppeteerRenderOutput type from puppeteer-renderer.js
  - GenerateSrtInputSchema, GenerateSrtOutputSchema, GenerateSrtInput type, GenerateSrtOutput type from srt-generator.js

The resulting index.ts should export ONLY these symbols:
  renderVideo, calculateTotalDuration, RenderVideoInputSchema, RenderVideoInput,
  SceneScriptSchema, ScriptOutputSchema, SceneScript, ScriptOutput,
  generateRemotionProject, GenerateProjectInput, GenerateProjectOutput,
  cleanupRemotionTempDir, generateSrt

After editing, verify with:
  grep -E "^export" packages/renderer/src/index.ts
  grep "renderVideoWithPuppeteer" packages/renderer/src/index.ts  (should find nothing)
  </action>
  <verify>
    <automated>grep -c "^export" packages/renderer/src/index.ts && ! grep -q "renderVideoWithPuppeteer\|PuppeteerRender\|GenerateSrtInputSchema\|GenerateSrtOutputSchema" packages/renderer/src/index.ts</automated>
  </verify>
  <done>index.ts exports only: renderVideo, calculateTotalDuration, RenderVideoInputSchema, RenderVideoInput, SceneScriptSchema, ScriptOutputSchema, SceneScript, ScriptOutput, generateRemotionProject, GenerateProjectInput, GenerateProjectOutput, cleanupRemotionTempDir, generateSrt</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned verification utilities file</name>
  <files>packages/renderer/src/verification/index.ts</files>
  <read_first>
    @packages/renderer/src/verification/index.ts
  </read_first>
  <action>
Delete packages/renderer/src/verification/index.ts since:
  - verifyShikiOutput, verifyContentIntegrity, verifyDurationMatch are not exported from index.ts
  - They are not imported anywhere in the codebase
  - They serve no current purpose

Before deleting, verify no imports exist:
  grep -r "verifyShikiOutput\|verifyContentIntegrity\|verifyDurationMatch" packages/renderer/src/ --include="*.ts" --include="*.tsx"

Expected: no matches found.

Then delete the file:
  rm packages/renderer/src/verification/index.ts

Also remove the verification directory if it becomes empty:
  ls packages/renderer/src/verification/
  rmdir packages/renderer/src/verification/ 2>/dev/null || true
  </action>
  <verify>
    <automated>test ! -f packages/renderer/src/verification/index.ts && test ! -d packages/renderer/src/verification/</automated>
  </verify>
  <done>packages/renderer/src/verification/index.ts deleted and verification directory removed</done>
</task>

</tasks>

<verification>
- Dead exports removed: RenderVideoOutputSchema, RenderVideoInput, RenderVideoOutput, renderVideoWithPuppeteer, PuppeteerRenderInputSchema, PuppeteerRenderOutputSchema, PuppeteerRenderInput, PuppeteerRenderOutput, GenerateSrtInputSchema, GenerateSrtOutputSchema, GenerateSrtInput, GenerateSrtOutput
- verifyShikiOutput, verifyContentIntegrity, verifyDurationMatch removed from orphaned file
- TypeScript compilation in packages/renderer still passes: cd packages/renderer && npx tsc --noEmit
</verification>

<success_criteria>
Dead export cleanup complete when:
- index.ts exports only used symbols (renderVideo, calculateTotalDuration, RenderVideoInputSchema, RenderVideoInput, SceneScriptSchema, ScriptOutputSchema, SceneScript, ScriptOutput, generateRemotionProject, GenerateProjectInput, GenerateProjectOutput, cleanupRemotionTempDir, generateSrt)
- verification/index.ts deleted and directory removed
- TypeScript compilation passes in packages/renderer
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-cleanup/08-03-SUMMARY.md`
</output>
