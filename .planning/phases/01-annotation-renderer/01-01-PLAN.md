---
phase: 01-annotation-renderer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/remotion/annotations/Underline.tsx
  - packages/renderer/src/remotion/annotations/Arrow.tsx
  - packages/renderer/src/remotion/annotations/Box.tsx
  - packages/renderer/src/remotion/annotations/Highlight.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "Underline renders a wobbly horizontal line below target region"
    - "Arrow renders a wobbly line with arrowhead pointing to target"
    - "Box renders a wobbly rectangle outline"
    - "Highlight renders a semi-transparent filled rectangle"
    - "All annotations use stroke-dashoffset draw-on animation"
    - "All annotations use spring with damping:100, stiffness:300"
    - "All interpolate calls use extrapolateRight: clamp"
  artifacts:
    - path: packages/renderer/src/remotion/annotations/Underline.tsx
      provides: Wobbly underline annotation
      exports: Underline, UnderlineProps
    - path: packages/renderer/src/remotion/annotations/Arrow.tsx
      provides: Wobbly arrow annotation with arrowhead
      exports: Arrow, ArrowProps
    - path: packages/renderer/src/remotion/annotations/Box.tsx
      provides: Wobbly rectangle box annotation
      exports: Box, BoxProps
    - path: packages/renderer/src/remotion/annotations/Highlight.tsx
      provides: Semi-transparent highlight rectangle
      exports: Highlight, HighlightProps
  key_links:
    - from: Underline.tsx
      to: annotations/index.ts
      via: imports generateWobblyPath, getAnnotationColor
    - from: Arrow.tsx
      to: annotations/index.ts
      via: imports generateWobblyPath, getAnnotationColor
    - from: Box.tsx
      to: annotations/index.ts
      via: imports generateWobblyPath, getAnnotationColor
    - from: Highlight.tsx
      to: annotations/index.ts
      via: imports generateWobblyPath, getAnnotationColor
---

<objective>
Create 4 stroke-based annotation components: Underline, Arrow, Box, and Highlight. Each follows the Circle.tsx reference pattern with hand-drawn wobbly style and stroke-dashoffset draw-on animation.

Purpose: Enable visual emphasis annotations on screenshots and code blocks
Output: 4 new annotation component files
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From packages/renderer/src/remotion/annotations/Circle.tsx (reference implementation):
```tsx
// Pattern: spring progress -> interpolate to strokeDashoffset
const progress = spring({
  frame: effectiveFrame,
  fps,
  config: { damping: 100, stiffness: 300 },
});
const strokeDashoffset = interpolate(progress, [0, 1], [pathLength, 0], {
  extrapolateRight: "clamp",
});
// SVG with overflow: "visible", path with strokeDasharray={pathLength}
```

From packages/renderer/src/remotion/annotations/index.ts:
```typescript
export function generateWobblyPath(points, wobble): string
export function getAnnotationColor(color: AnnotationColor): string
export const ANNOTATION_COLORS = { attention, highlight, info, success }
```

From src/types/visual.ts:
```typescript
export type AnnotationColor = "attention" | "highlight" | "info" | "success"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Underline.tsx</name>
  <files>packages/renderer/src/remotion/annotations/Underline.tsx</files>
  <read_first>
    packages/renderer/src/remotion/annotations/Circle.tsx
    packages/renderer/src/remotion/annotations/index.ts
  </read_first>
  <action>
    Create Underline.tsx following Circle.tsx pattern:

    1. Import useCurrentFrame, useVideoConfig, interpolate, spring from "remotion"
    2. Import getAnnotationColor, generateWobblyPath from "./index.js"
    3. Import type AnnotationColor from "@video-script/types"

    4. Define UnderlineProps interface:
       - x: number (start X)
       - y: number (start Y)
       - width: number (line width in pixels)
       - color: AnnotationColor
       - strokeWidth?: number (default 3)
       - wobble?: number (default 3)
       - appearAt?: number (default 0)

    5. Generate wobbly path:
       - points = [{x: x, y: y + 5}, {x: x + width, y: y + 5}]  // 5px below for underline
       - path = generateWobblyPath(points, wobble)
       - pathLength = width (straight line)

    6. Use spring({ damping: 100, stiffness: 300 }) + interpolate with extrapolateRight: "clamp"

    7. Return SVG with path, stroke={getAnnotationColor(color)}, fill="none"
       - SVG overflow: "visible"
       - Include drop-shadow filter

    8. Export component and props interface
  </action>
  <verify>
    <automated>grep -l "export.*Underline" packages/renderer/src/remotion/annotations/Underline.tsx && grep -l "spring" packages/renderer/src/remotion/annotations/Underline.tsx && grep -l "extrapolateRight" packages/renderer/src/remotion/annotations/Underline.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/annotations/Underline.tsx exists
    - Contains "export const Underline"
    - Contains "spring({ damping: 100, stiffness: 300 })"
    - Contains "extrapolateRight: \"clamp\""
    - Contains "generateWobblyPath"
    - Contains "strokeDasharray={pathLength}"
    - Contains "overflow: \"visible\""
  </acceptance_criteria>
  <done>Underline annotation renders a wobbly horizontal line with stroke-dashoffset draw-on animation</done>
</task>

<task type="auto">
  <name>Task 2: Create Arrow.tsx</name>
  <files>packages/renderer/src/remotion/annotations/Arrow.tsx</files>
  <read_first>
    packages/renderer/src/remotion/annotations/Circle.tsx
    packages/renderer/src/remotion/annotations/index.ts
  </read_first>
  <action>
    Create Arrow.tsx following Circle.tsx pattern:

    1. Import useCurrentFrame, useVideoConfig, interpolate, spring from "remotion"
    2. Import getAnnotationColor, generateWobblyPath from "./index.js"
    3. Import type AnnotationColor from "@video-script/types"

    4. Define ArrowProps interface:
       - x1: number (start X)
       - y1: number (start Y)
       - x2: number (end X)
       - y2: number (end Y)
       - color: AnnotationColor
       - strokeWidth?: number (default 3)
       - wobble?: number (default 3)
       - appearAt?: number (default 0)

    5. Generate wobbly line path:
       - bodyPoints = [{x: x1, y: y1}, {x: x2, y: y2}]
       - bodyPath = generateWobblyPath(bodyPoints, wobble)
       - pathLength = Math.sqrt((x2-x1)**2 + (y2-y1)**2)

    6. Arrowhead: Create separate path or use marker-end attribute
       - Option: Draw small wobbly lines for arrowhead at (x2, y2)
       - arrowSize = 10
       - Arrowhead lines: from (x2, y2) to (x2 - arrowSize, y2 + arrowSize/2) and (x2 - arrowSize, y2 - arrowSize/2)

    7. Use spring({ damping: 100, stiffness: 300 }) + interpolate with extrapolateRight: "clamp"

    8. Return SVG containing:
       - Main line path with strokeDasharray and strokeDashoffset animation
       - Arrowhead paths (NOT animated, or same animation)
       - stroke={getAnnotationColor(color)}, fill="none"
       - overflow: "visible", include drop-shadow

    9. Export component and props interface
  </action>
  <verify>
    <automated>grep -l "export.*Arrow" packages/renderer/src/remotion/annotations/Arrow.tsx && grep -l "spring" packages/renderer/src/remotion/annotations/Arrow.tsx && grep -l "extrapolateRight" packages/renderer/src/remotion/annotations/Arrow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/annotations/Arrow.tsx exists
    - Contains "export const Arrow"
    - Contains "spring({ damping: 100, stiffness: 300 })"
    - Contains "extrapolateRight: \"clamp\""
    - Contains "generateWobblyPath"
    - Contains "x1", "y1", "x2", "y2" props
    - Contains "overflow: \"visible\""
  </acceptance_criteria>
  <done>Arrow annotation renders a wobbly line with arrowhead using stroke-dashoffset draw-on animation</done>
</task>

<task type="auto">
  <name>Task 3: Create Box.tsx</name>
  <files>packages/renderer/src/remotion/annotations/Box.tsx</files>
  <read_first>
    packages/renderer/src/remotion/annotations/Circle.tsx
    packages/renderer/src/remotion/annotations/index.ts
  </read_first>
  <action>
    Create Box.tsx following Circle.tsx pattern:

    1. Import useCurrentFrame, useVideoConfig, interpolate, spring from "remotion"
    2. Import getAnnotationColor, generateWobblyPath from "./index.js"
    3. Import type AnnotationColor from "@video-script/types"

    4. Define BoxProps interface:
       - x: number (left edge)
       - y: number (top edge)
       - width: number (box width)
       - height: number (box height)
       - color: AnnotationColor
       - strokeWidth?: number (default 3)
       - wobble?: number (default 3)
       - appearAt?: number (default 0)

    5. Generate wobbly rectangle path (closed path):
       - points = [{x, y}, {x: x+width, y}, {x: x+width, y: y+height}, {x, y: y+height}, {x, y}]
       - path = generateWobblyPath(points, wobble)
       - pathLength = 2 * (width + height) (perimeter)

    6. Use spring({ damping: 100, stiffness: 300 }) + interpolate with extrapolateRight: "clamp"

    7. Return SVG with path, stroke={getAnnotationColor(color)}, fill="none"
       - SVG overflow: "visible"
       - Position SVG to encompass full box with padding
       - Include drop-shadow filter

    8. Export component and props interface
  </action>
  <verify>
    <automated>grep -l "export.*Box" packages/renderer/src/remotion/annotations/Box.tsx && grep -l "spring" packages/renderer/src/remotion/annotations/Box.tsx && grep -l "extrapolateRight" packages/renderer/src/remotion/annotations/Box.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/annotations/Box.tsx exists
    - Contains "export const Box"
    - Contains "spring({ damping: 100, stiffness: 300 })"
    - Contains "extrapolateRight: \"clamp\""
    - Contains "generateWobblyPath"
    - Contains "strokeDasharray={pathLength}"
    - Contains "overflow: \"visible\""
  </acceptance_criteria>
  <done>Box annotation renders a wobbly rectangle outline with stroke-dashoffset draw-on animation</done>
</task>

<task type="auto">
  <name>Task 4: Create Highlight.tsx</name>
  <files>packages/renderer/src/remotion/annotations/Highlight.tsx</files>
  <read_first>
    packages/renderer/src/remotion/annotations/Circle.tsx
    packages/renderer/src/remotion/annotations/index.ts
  </read_first>
  <action>
    Create Highlight.tsx following Circle.tsx pattern:

    1. Import useCurrentFrame, useVideoConfig, interpolate, spring from "remotion"
    2. Import getAnnotationColor, generateWobblyPath from "./index.js"
    3. Import type AnnotationColor from "@video-script/types"

    4. Define HighlightProps interface:
       - x: number (left edge)
       - y: number (top edge)
       - width: number (highlight width)
       - height: number (highlight height)
       - color: AnnotationColor (typically "highlight" yellow)
       - opacity?: number (default 0.3 for semi-transparent fill)
       - appearAt?: number (default 0)

    5. Generate wobbly rectangle path for fill (slightly inset to avoid stroke overlap):
       - inset = 2
       - points = [{x: x+inset, y: y+inset}, {x: x+width-inset, y: y+inset}, {x: x+width-inset, y: y+height-inset}, {x: x+inset, y: y+height-inset}, {x: x+inset, y: y+inset}]
       - path = generateWobblyPath(points, wobble=2) // less wobble for fill
       - pathLength = 2 * ((width-2*inset) + (height-2*inset))

    6. Use spring({ damping: 100, stiffness: 300 }) + interpolate with extrapolateRight: "clamp"
       - Note: Highlight uses opacity fade-in for fill, not stroke-dashoffset (per D-02 all use stroke-dashoffset)
       - Actually per D-02: "All annotation types use the same stroke-dashoffset draw-on animation"
       - So use stroke-dashoffset for the filled rectangle perimeter too

    7. Return SVG with path:
       - fill={getAnnotationColor(color)} with opacity or use fill opacity
       - stroke="none" (no stroke for highlight)
       - Actually per Circle pattern and D-02, use stroke-dashoffset on fill path
       - Set stroke={getAnnotationColor(color)} strokeWidth={height} for thick underline effect
       - Or use fill with opacity fade-in

    8. Per D-02 all use stroke-dashoffset, so create filled rectangle:
       - stroke={getAnnotationColor(color)} strokeWidth={height} (makes thick "marker" line)
       - strokeDasharray={pathLength} strokeDashoffset={animated}

    9. SVG overflow: "visible", position absolutely

    10. Export component and props interface
  </action>
  <verify>
    <automated>grep -l "export.*Highlight" packages/renderer/src/remotion/annotations/Highlight.tsx && grep -l "spring" packages/renderer/src/remotion/annotations/Highlight.tsx && grep -l "extrapolateRight" packages/renderer/src/remotion/annotations/Highlight.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/annotations/Highlight.tsx exists
    - Contains "export const Highlight"
    - Contains "spring({ damping: 100, stiffness: 300 })"
    - Contains "extrapolateRight: \"clamp\""
    - Contains "generateWobblyPath"
    - Contains "fill=" (uses fill, not just stroke)
    - Contains "overflow: \"visible\""
  </acceptance_criteria>
  <done>Highlight annotation renders a semi-transparent filled rectangle with stroke-dashoffset animation</done>
</task>

</tasks>

<verification>
- All 4 files exist in packages/renderer/src/remotion/annotations/
- All 4 use spring({ damping: 100, stiffness: 300 })
- All 4 use extrapolateRight: "clamp"
- All 4 import from "./index.js" (generateWobblyPath, getAnnotationColor)
</verification>

<success_criteria>
- Underline.tsx, Arrow.tsx, Box.tsx, Highlight.tsx all created
- Each uses stroke-dashoffset draw-on animation pattern from Circle.tsx
- Each uses spring with damping:100, stiffness:300
- Each uses extrapolateRight: "clamp"
- Each reuses generateWobblyPath and getAnnotationColor from index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-renderer/01-01-SUMMARY.md`
</output>
