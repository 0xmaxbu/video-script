---
phase: 01-annotation-renderer
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - packages/renderer/src/remotion/Scene.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "Scene component accepts annotations prop"
    - "Scene renders AnnotationRenderer with annotations array"
    - "Annotations layer correctly over visual content"
  artifacts:
    - path: packages/renderer/src/remotion/Scene.tsx
      provides: Updated Scene component with annotation support
      updates: SceneProps interface and Scene component implementation
  key_links:
    - from: Scene.tsx
      to: AnnotationRenderer.tsx
      via: Imports and renders AnnotationRenderer with annotations prop
---

<objective>
Update Scene.tsx to accept an annotations prop and render AnnotationRenderer. This integrates the annotation system into the scene rendering pipeline.

Purpose: Enable annotations to render over scene content (screenshots, code blocks)
Output: Updated Scene.tsx with annotations prop and AnnotationRenderer rendering
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From packages/renderer/src/remotion/Scene.tsx (current implementation):
```typescript
interface SceneProps {
  scene: SceneType;
  imagePaths: Record<string, string> | undefined;
}
// Scene uses visualLayers to render content
// AbsoluteFill wraps the scene content
```

From packages/renderer/src/remotion/annotations/AnnotationRenderer.tsx (creating in 01-03):
```typescript
interface AnnotationRendererProps {
  annotations: Annotation[];
}
// Renders all 6 annotation types sorted by appearAt
```

From packages/renderer/src/types.ts:
```typescript
export const SceneScriptSchema = z.object({
  id: z.string(),
  type: SceneNarrativeType,
  title: z.string(),
  narration: z.string(),
  duration: z.number().positive(),
  visualLayers: z.array(VisualLayerSchema).optional(),
  transition: SceneTransitionSchema.optional(),
});
```

Note: Per D-11, Scene receives annotations as a direct prop (not via visualLayers). The Annotation type comes from @video-script/types.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Scene.tsx with annotations prop</name>
  <files>packages/renderer/src/remotion/Scene.tsx</files>
  <read_first>
    packages/renderer/src/remotion/Scene.tsx
    packages/renderer/src/remotion/annotations/AnnotationRenderer.tsx
  </read_first>
  <action>
    Update Scene.tsx to integrate annotations:

    1. Add import for AnnotationRenderer:
       - import { AnnotationRenderer } from "./annotations/AnnotationRenderer.js"

    2. Add import for Annotation type:
       - import type { Annotation } from "@video-script/types"

    3. Update SceneProps interface:
       - Add annotations?: Annotation[] prop (optional, defaults to empty array)

    4. Destructure annotations from props:
       - const { scene, imagePaths, annotations = [] } = props;

    5. Add AnnotationRenderer rendering in each scene type branch:
       - For intro/outro, feature, and code scenes:
       - After the visual layers rendering and before or after Subtitle
       - Place inside AbsoluteFill, after visualLayers.map() but before closing tag

    6. Specifically, for each scene type (intro/outro, feature, code):
       - Add <AnnotationRenderer annotations={annotations} />
       - This renders annotations over the visual content

    7. Ensure annotations render on top (later in JSX = higher z-index in AbsoluteFill):
       - Place after visualLayers content but before or after Subtitle
       - The Subtitle is at bottom with zIndex consideration

    8. Export updated component
  </action>
  <verify>
    <automated>grep -l "AnnotationRenderer" packages/renderer/src/remotion/Scene.tsx && grep -l "annotations" packages/renderer/src/remotion/Scene.tsx && grep -l "Annotation" packages/renderer/src/remotion/Scene.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/Scene.tsx imports AnnotationRenderer
    - packages/renderer/src/remotion/Scene.tsx imports Annotation type
    - SceneProps interface includes annotations?: Annotation[]
    - Scene component destructures annotations from props
    - AnnotationRenderer renders in each scene type (intro/outro, feature, code)
  </acceptance_criteria>
  <done>Scene.tsx accepts annotations prop and renders AnnotationRenderer over scene content</done>
</task>

</tasks>

<verification>
- Scene.tsx updated with annotations prop
- AnnotationRenderer imported and rendered
- TypeScript compiles without errors (typecheck passes)
</verification>

<success_criteria>
- Scene component accepts annotations prop
- AnnotationRenderer renders with annotations array
- Annotations layer over visual content in scene
- Phase 1 complete: all 6 annotation types renderable via Scene component
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-renderer/01-04-SUMMARY.md`
</output>
