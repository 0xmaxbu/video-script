---
phase: 01-annotation-renderer
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/remotion/annotations/Number.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "Number renders a circle with a number inside"
    - "Number uses same spring animation as other annotations"
    - "Number is positioned at x, y with specified radius"
  artifacts:
    - path: packages/renderer/src/remotion/annotations/Number.tsx
      provides: Circle annotation with number label
      exports: Number, NumberProps
  key_links:
    - from: Number.tsx
      to: Circle.tsx
      via: Reuses circle rendering logic with number overlay
    - from: Number.tsx
      to: annotations/index.ts
      via: imports generateWobblyPath, getAnnotationColor
---

<objective>
Create Number.tsx - a circle annotation with a number rendered inside. This is a variant of Circle that adds a text element showing the step number.

Purpose: Enable numbered step annotations for tutorial sequences
Output: Number.tsx component
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
From packages/renderer/src/remotion/annotations/Circle.tsx (reference):
```tsx
// Circle renders a wobbly circle path with stroke-dashoffset animation
// Uses spring + interpolate with extrapolateRight: "clamp"
```

From packages/renderer/src/remotion/annotations/index.ts:
```typescript
export function generateWobblyPath(points, wobble): string
export function getAnnotationColor(color: AnnotationColor): string
```

From src/types/visual.ts:
```typescript
export type AnnotationColor = "attention" | "highlight" | "info" | "success"
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Number.tsx</name>
  <files>packages/renderer/src/remotion/annotations/Number.tsx</files>
  <read_first>
    packages/renderer/src/remotion/annotations/Circle.tsx
    packages/renderer/src/remotion/annotations/index.ts
  </read_first>
  <action>
    Create Number.tsx combining Circle rendering with text overlay:

    1. Import useCurrentFrame, useVideoConfig, interpolate, spring from "remotion"
    2. Import getAnnotationColor, generateWobblyPath from "./index.js"
    3. Import type AnnotationColor from "@video-script/types"

    4. Define NumberProps interface:
       - x: number (center X)
       - y: number (center Y)
       - n: number (the number to display, 1-99)
       - color: AnnotationColor
       - strokeWidth?: number (default 3)
       - wobble?: number (default 3)
       - appearAt?: number (default 0)

    5. Calculate same wobbly circle path as Circle.tsx:
       - radius = 20 (fixed for number circle)
       - points array around circle
       - path = generateWobblyPath(points, wobble)
       - pathLength = 2 * Math.PI * radius

    6. Spring animation same as Circle:
       - progress = spring({ frame: effectiveFrame, fps, config: { damping: 100, stiffness: 300 } })
       - strokeDashoffset = interpolate(progress, [0, 1], [pathLength, 0], { extrapolateRight: "clamp" })

    7. Return SVG with:
       - Circle path with strokeDasharray/strokeDashoffset (same as Circle)
       - fill="none"
       - stroke={getAnnotationColor(color)}
       - Drop shadow filter
       - Text element inside circle:
         - <text x={x} y={y} textAnchor="middle" dominantBaseline="central"
         - fontSize={radius * 0.8} fontWeight="bold" fill={getAnnotationColor(color)}
         - Fill opacity: if color is "highlight" use "black", else use "white" for contrast
         - Content: String(n)
       - overflow: "visible"

    8. Export component and props interface
  </action>
  <verify>
    <automated>grep -l "export.*Number" packages/renderer/src/remotion/annotations/Number.tsx && grep -l "spring" packages/renderer/src/remotion/annotations/Number.tsx && grep -l "textAnchor" packages/renderer/src/remotion/annotations/Number.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/renderer/src/remotion/annotations/Number.tsx exists
    - Contains "export const Number"
    - Contains "spring({ damping: 100, stiffness: 300 })"
    - Contains "extrapolateRight: \"clamp\""
    - Contains "generateWobblyPath"
    - Contains "<text" element with number rendering
    - Contains "textAnchor=\"middle\""
    - Contains "overflow: \"visible\""
  </acceptance_criteria>
  <done>Number annotation renders a wobbly circle with centered number inside</done>
</task>

</tasks>

<verification>
- Number.tsx exists in packages/renderer/src/remotion/annotations/
- Uses spring({ damping: 100, stiffness: 300 })
- Uses extrapolateRight: "clamp"
- Renders text element with the number
</verification>

<success_criteria>
- Number.tsx created following Circle.tsx pattern
- Number displays inside wobbly circle
- Spring animation applied to circle stroke
- Text centered within circle
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-renderer/01-02-SUMMARY.md`
</output>
