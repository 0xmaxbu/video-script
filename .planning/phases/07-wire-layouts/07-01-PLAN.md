---
phase: 07-wire-layouts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/types.ts
autonomous: true
requirements: [VIS-04, VIS-05, VIS-06, VIS-07]
must_haves:
  truths:
    - "SceneScriptSchema includes optional layoutTemplate field"
    - "layoutTemplate accepts all 9 layout values plus inline fallback"
    - "TypeScript compilation succeeds with new field"
  artifacts:
    - path: "packages/renderer/src/types.ts"
      provides: "SceneScriptSchema with layoutTemplate field"
      contains: "layoutTemplate:"
  key_links:
    - from: "packages/renderer/src/types.ts"
      to: "SceneScript type"
      via: "zod schema extension"
      pattern: "layoutTemplate.*z.enum"
---

<objective>
Add optional layoutTemplate field to SceneScriptSchema enabling agent-driven layout selection.

Purpose: Allow script-agent to specify which professional layout template to use for each scene (D-02).
Output: SceneScriptSchema with layoutTemplate field supporting 9 layout templates + inline fallback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-wire-layouts/07-CONTEXT.md
@.planning/phases/07-wire-layouts/07-RESEARCH.md

<interfaces>
<!-- Key types and existing schema structure -->

From packages/renderer/src/types.ts (current SceneScriptSchema):
```typescript
export const SceneScriptSchema = z.object({
  id: z.string(),
  type: SceneNarrativeType,
  title: z.string(),
  narration: z.string(),
  duration: z.number().positive(),
  visualLayers: z.array(VisualLayerSchema).optional(),
  transition: SceneTransitionSchema.optional(),
  highlights: z.array(SceneHighlightSchema).optional(),
  codeHighlights: z.array(CodeHighlightSchema).optional(),
  sourceRef: z.string().optional(),
});
```

From packages/renderer/src/remotion/layouts/index.ts (layout template values):
```typescript
// Template strings handled by getLayoutComponent():
// "hero-fullscreen", "split-horizontal", "split-vertical",
// "text-over-image", "code-focus", "comparison",
// "bullet-list", "quote"
```

From src/types/visual.ts (LayoutTemplateEnum reference):
```typescript
export const LayoutTemplateEnum = z.enum([
  "hero-fullscreen",
  "split-horizontal",
  "split-vertical",
  "text-over-image",
  "code-focus",
  "comparison",
  "bullet-list",
  "quote",
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add layoutTemplate field to SceneScriptSchema</name>
  <files>packages/renderer/src/types.ts</files>
  <read_first>
    - packages/renderer/src/types.ts (current schema structure)
    - packages/renderer/src/remotion/layouts/index.ts (valid template names)
  </read_first>
  <action>
    Add optional `layoutTemplate` field to SceneScriptSchema in packages/renderer/src/types.ts.

    Per D-02a, the field must accept these exact values:
    - "hero-fullscreen"
    - "split-horizontal"
    - "split-vertical"
    - "text-over-image"
    - "code-focus"
    - "comparison"
    - "bullet-list"
    - "quote"
    - "inline" (for explicit fallback, per D-02b)

    Add this field after the existing `sourceRef` field:

    ```typescript
    // D-02: Optional layout template selection
    layoutTemplate: z.enum([
      "hero-fullscreen",
      "split-horizontal",
      "split-vertical",
      "text-over-image",
      "code-focus",
      "comparison",
      "bullet-list",
      "quote",
      "inline",
    ]).optional(),
    ```

    Also export a LayoutTemplate type alias for use in other files:

    ```typescript
    export type LayoutTemplate = z.infer<typeof LayoutTemplateEnum>;
    ```

    Where LayoutTemplateEnum is defined as:

    ```typescript
    const LayoutTemplateEnum = z.enum([
      "hero-fullscreen",
      "split-horizontal",
      "split-vertical",
      "text-over-image",
      "code-focus",
      "comparison",
      "bullet-list",
      "quote",
      "inline",
    ]);
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/renderer/tsconfig.json 2>&1 | grep -c "error" | xargs -I{} bash -c 'if [ "{}" = "0" ]; then exit 0; else exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - SceneScriptSchema contains `layoutTemplate` field with `.optional()`
    - layoutTemplate z.enum includes exactly 9 values: hero-fullscreen, split-horizontal, split-vertical, text-over-image, code-focus, comparison, bullet-list, quote, inline
    - TypeScript compilation succeeds with 0 errors
    - SceneScript type (z.infer) includes layoutTemplate?: string
  </acceptance_criteria>
  <done>SceneScriptSchema has optional layoutTemplate field; TypeScript compiles successfully.</done>
</task>

</tasks>

<verification>
- Run `npx tsc --noEmit -p packages/renderer/tsconfig.json` — must pass with 0 errors
- Grep for `layoutTemplate:` in packages/renderer/src/types.ts — must find the field
- Grep for `"inline"` in the enum — must find the fallback value
</verification>

<success_criteria>
- SceneScriptSchema.layoutTemplate field exists and is optional
- All 9 layout template values are accepted (8 layouts + inline fallback)
- TypeScript compilation succeeds
- Field is properly typed via z.infer
</success_criteria>

<output>
After completion, create `.planning/phases/07-wire-layouts/07-01-SUMMARY.md`
</output>
