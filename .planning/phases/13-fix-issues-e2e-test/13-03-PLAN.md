# Plan 13-03: Pipeline Tests for Compose Step

**Phase:** 13-fix-issues-e2e-test
**Goal:** Add integration tests for the compose pipeline using fixture data (no LLM needed)

## Tasks

### Task 1: Create test fixtures

**Directory:** `src/mastra/agents/__tests__/fixtures/`
**Files to create:**

- `sample-script.json` — minimal ScriptOutput with 2 scenes (intro + feature)
- `sample-visual.json` — VisualPlan with mediaResources and textElements for both scenes
- `sample-screenshots/` — empty directory with 2 mock PNG files (scene-0-hero.png, scene-1-article.png)

### Task 2: Test compose-agent helper functions

**File:** `src/mastra/agents/__tests__/compose-agent.test.ts`

Tests:

1. `mapLayoutToComponent("hero-fullscreen")` → "HeroFullscreen" (all 8 templates)
2. `mapLayoutToComponent("unknown")` → "HeroFullscreen" (default)
3. `secondsToFrames(10, 30)` → 300
4. `secondsToFrames(0, 30)` → 0
5. `secondsToFrames(5.5, 30)` → 165

### Task 3: Test scene-adapter with visualLayers

**File:** `src/utils/__tests__/scene-adapter-visual.test.ts`

Tests:

1. `createTextElements` with visualLayers converts type="text" layers to TextElements
2. `createTextElements` with empty visualLayers returns empty array
3. `textElementToVisualLayer` maps all 5 positions correctly (left, right, top, center, bottom)
4. `mediaResourceToVisualLayer` returns type="screenshot" with correct fields
5. `adaptScriptForRenderer` merges visual.json mediaResources + textElements into visualLayers
6. `adaptScriptForRenderer` without visual.json preserves existing visualLayers

### Task 4: Test findScreenshotFile logic

**File:** `src/cli/__tests__/screenshot-mapping.test.ts`

Note: `findScreenshotFile` is a local function in cli/index.ts. If not exported, extract it to `src/utils/screenshot-finder.ts` first, then test.

Tests:

1. Exact match: `{layerId}.png` found
2. Scene-prefixed: `scene-XXX-{layerId}.png` found
3. Fallback: any file starting with scene prefix
4. No match: returns undefined/null

## Verification

- All tests pass: `npx vitest run src/mastra/agents/__tests__/compose-agent.test.ts src/utils/__tests__/scene-adapter-visual.test.ts src/cli/__tests__/screenshot-mapping.test.ts`
- No LLM calls needed
- Fixtures are minimal and readable
