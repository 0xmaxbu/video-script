---
phase: 11-screenshot-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/tools/playwright-screenshot.ts
  - src/mastra/agents/screenshot-agent.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Screenshot Agent can analyze page structure using LLM"
    - "AI-generated selectors are more precise than DEFAULT_SELECTORS"
    - "Screenshot tool accepts and uses AI-generated selectors"
  artifacts:
    - path: src/mastra/tools/playwright-screenshot.ts
      provides: Screenshot capture with selector support
      min_lines: 100
    - path: src/mastra/agents/screenshot-agent.ts
      provides: Screenshot Agent with AI-guided selector generation
      min_lines: 213
  key_links:
    - from: src/mastra/agents/screenshot-agent.ts
      to: src/mastra/tools/playwright-screenshot.ts
      via: playwrightScreenshotTool with selector parameter
---

<objective>
Add AI-guided selector generation to Screenshot Agent so it analyzes page content and generates precise CSS selectors instead of using generic defaults.

Purpose: Solve the problem of generic CSS selectors capturing irrelevant content
Output: Updated screenshot-agent.ts with LLM-based selector generation
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-screenshot-quality/11-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@src/mastra/tools/playwright-screenshot.ts
@src/mastra/agents/screenshot-agent.ts
@src/mastra/agents/visual-agent.ts

**Context Decisions (locked - implement exactly as specified):**

- D-01: AI-guided selection — LLM analyzes page content and generates precise CSS selector for the relevant section
- D-02: Screenshot Agent analyzes page structure when capturing — Visual Agent plans layout, Screenshot Agent refines selectors at capture time
- D-04: Type-specific strategies — Different approaches per content type:
  - Documentation: text-focused selectors (article, .content, .markdown-body)
  - Code: syntax-highlighted blocks (pre, code, .highlight)
  - Articles: semantic section detection (h1, h2, main sections)

**Agent Discretion (implement reasonably):**

- Exact retry logic and max retry count
- How to extract selector suggestions from page analysis
- Fallback selector priority ordering
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add analyzePageStructure tool to playwright-screenshot</name>
  <files>src/mastra/tools/playwright-screenshot.ts</files>
  <read_first>
    - src/mastra/tools/playwright-screenshot.ts (current implementation)
  </read_first>
  <action>
    Add a new function `analyzePageStructure` to playwright-screenshot.ts that:
    - Takes a URL and optional content-type hint (documentation | code | article)
    - Uses Playwright to load the page and extract semantic structure
    - Returns structured data about page sections: { headings: string[], links: string[], codeBlocks: string[], semanticRegions: { name: string, selector: string }[] }
    - Uses page.evaluate() to extract page structure
    - Throws on network failure or timeout (60 seconds max)
    
    Add this as a named export function (not a Mastra tool) that Screenshot Agent can call directly.
  </action>
  <verify>
    <automated>grep -n "export.*analyzePageStructure\|export function analyzePageStructure" src/mastra/tools/playwright-screenshot.ts</automated>
  </verify>
  <done>analyzePageStructure function exists and exports correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add LLM-guided selector generation to screenshot-agent</name>
  <files>src/mastra/agents/screenshot-agent.ts</files>
  <read_first>
    - src/mastra/agents/screenshot-agent.ts (current implementation with DEFAULT_SELECTORS)
  </read_first>
  <action>
    Add a new function `generateAISelector` to screenshot-agent.ts that:
    - Takes: { url: string, contentHint: string, narrationContext: string }
    - Calls analyzePageStructure(url, contentHint) to get page structure
    - Uses the agent's LLM to analyze the structure and generate a precise CSS selector based on:
      * The narration context (what content is being discussed)
      * The content type hint (documentation/code/article)
      * The actual page structure
    - Returns a precise CSS selector string
    - Has smart fallback: if AI fails, returns appropriate DEFAULT_SELECTORS entry

    The agent instructions already exist - add the generateAISelector function that the agent can call. The agent should call this before calling playwrightScreenshotTool for informational screenshots.

  </action>
  <verify>
    <automated>grep -n "generateAISelector\|async function generateAISelector" src/mastra/agents/screenshot-agent.ts</automated>
  </verify>
  <done>generateAISelector function exists in screenshot-agent.ts</done>
</task>

<task type="auto">
  <name>Task 3: Update agent instructions to use AI-guided selectors</name>
  <files>src/mastra/agents/screenshot-agent.ts</files>
  <read_first>
    - src/mastra/agents/screenshot-agent.ts (instructions section lines 16-94)
  </read_first>
  <action>
    Update the agent instructions to:
    1. For informational screenshots (headline, article, documentation, codeSnippet, changelog, feature):
       - First: call generateAISelector({ url, contentHint: type, narrationContext: contextFromVisualPlan })
       - Then: use the returned selector when calling playwrightScreenshotTool
    2. Add instruction: "IMPORTANT: Always prefer AI-generated selectors over DEFAULT_SELECTORS when available"
    3. Keep the fallback strategy (DEFAULT_SELECTORS) if AI selector fails

    The visual plan should contain narration context - extract it from scene.narration or scene.title.

  </action>
  <verify>
    <automated>grep -n "generateAISelector\|AI-generated selector" src/mastra/agents/screenshot-agent.ts</automated>
  </verify>
  <done>Agent instructions reference generateAISelector for informational screenshots</done>
</task>

</tasks>

<verification>
- analyzePageStructure exported from playwright-screenshot.ts
- generateAISelector exists in screenshot-agent.ts
- Agent instructions updated to use AI-guided selectors
- TypeScript compiles without errors
</verification>

<success_criteria>
Screenshot Agent uses LLM-generated selectors for informational screenshots, falling back to DEFAULT_SELECTORS only on failure
</success_criteria>

<output>
After completion, create `.planning/phases/11-screenshot-quality/11-01-SUMMARY.md`
</output>
