---
phase: 11-screenshot-quality
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mastra/tools/playwright-screenshot.ts
  - src/mastra/agents/screenshot-agent.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Screenshot failures trigger AI analysis of why selector failed"
    - "Retry uses refined selector based on failure analysis"
    - "ORB blocking is mitigated by using local file approach"
  artifacts:
    - path: src/mastra/tools/playwright-screenshot.ts
      provides: Screenshot capture with retry support
    - path: src/mastra/agents/screenshot-agent.ts
      provides: Intelligent retry with AI refinement
  key_links:
    - from: src/mastra/agents/screenshot-agent.ts
      to: src/mastra/tools/playwright-screenshot.ts
      via: Retry with improved selectors on failure
---

<objective>
Add intelligent retry with AI refinement and ORB blocking mitigation.

Purpose: When screenshots fail or capture irrelevant content, the system should analyze why and retry with improved selectors. Also handle ORB blocking by ensuring screenshots use local file references.
Output: Updated retry logic in screenshot tools and agent
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-screenshot-quality/11-CONTEXT.md
@.planning/phases/11-screenshot-quality/11-01-PLAN.md
@src/mastra/tools/playwright-screenshot.ts
@src/mastra/agents/screenshot-agent.ts

**Context Decisions (locked - implement exactly as specified):**

- D-03: Retry with AI refinement — When screenshot fails or content isn't relevant, Screenshot Agent analyzes why and retries with improved selector

**Agent Discretion (implement reasonably):**

- Exact retry logic and max retry count
- Fallback selector priority ordering

**ORB Blocking Context (from CONTEXT.md deferred):**

- ORB blocking fix — Remote URL images blocked by browser; use local screenshot files only
- This means: ensure screenshot output uses local file paths, not remote URLs
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Add analyzeFailureAndSuggestSelector function</name>
  <files>src/mastra/agents/screenshot-agent.ts</files>
  <read_first>
    - src/mastra/agents/screenshot-agent.ts (current implementation)
  </read_first>
  <action>
    Add a new function `analyzeFailureAndSuggestSelector` to screenshot-agent.ts that:
    - Takes: { url: string, failedSelector: string, errorMessage: string, contentHint: string }
    - Calls analyzePageStructure to get current page structure
    - Uses LLM to analyze:
      * Why the failed selector didn't work (element moved, wrong format, etc.)
      * What content is actually relevant on the page given the error context
      * Suggest improved selector based on actual page content
    - Returns: { improvedSelector: string, reason: string, alternativeSelectors: string[] }
    - Max 2 suggestions for alternative selectors

    This function enables the "Retry with AI refinement" decision (D-03).

  </action>
  <verify>
    <automated>grep -n "analyzeFailureAndSuggestSelector" src/mastra/agents/screenshot-agent.ts</automated>
  </verify>
  <done>analyzeFailureAndSuggestSelector function exists and is exported</done>
</task>

<task type="auto">
  <name>Task 2: Implement intelligent retry wrapper for screenshot capture</name>
  <files>src/mastra/agents/screenshot-agent.ts</files>
  <read_first>
    - src/mastra/agents/screenshot-agent.ts (retry context from Task 1)
  </read_first>
  <action>
    Add a new function `captureWithRetry` to screenshot-agent.ts that:
    - Takes: { url, selector, contentHint, narrationContext, maxRetries: 2 }
    - Attempts screenshot with playwrightScreenshotTool
    - On failure: calls analyzeFailureAndSuggestSelector to get improved selector
    - Retries with improved selector (up to maxRetries times)
    - Returns: { imagePath, selectorUsed, success, attempts }
    - Logs each attempt's selector and failure reason

    This implements D-03: "Retry with AI refinement — When screenshot fails or content isn't relevant, Screenshot Agent analyzes why and retries with improved selector"

  </action>
  <verify>
    <automated>grep -n "captureWithRetry" src/mastra/agents/screenshot-agent.ts</automated>
  </verify>
  <done>captureWithRetry function exists and handles retry logic</done>
</task>

<task type="auto">
  <name>Task 3: Ensure ORB blocking mitigation - local file paths only</name>
  <files>src/mastra/tools/playwright-screenshot.ts</files>
  <read_first>
    - src/mastra/tools/playwright-screenshot.ts (output handling)
  </read_first>
  <action>
    Review and ensure playwright-screenshot.ts:
    1. Always saves screenshots to local filesystem (outputDir) - already does this
    2. Returns imagePath as local path, NOT remote URL
    3. Document in JSDoc that returned imagePath is always local

    The ORB blocking issue is about remote URLs being blocked by the browser when capturing. The solution is ensuring screenshot tool:
    - Downloads content locally before capturing (already does via Playwright)
    - Returns only local file paths (already does)

    Add explicit JSDoc comment noting this is ORB-safe.

  </action>
  <verify>
    <automated>grep -n "ORB\|local\|imagePath" src/mastra/tools/playwright-screenshot.ts | head -10</automated>
  </verify>
  <done>ORB mitigation documented - screenshots use local file paths</done>
</task>

</tasks>

<verification>
- analyzeFailureAndSuggestSelector exists and exported
- captureWithRetry implements retry with AI refinement
- ORB mitigation documented
- TypeScript compiles without errors
</verification>

<success_criteria>
Screenshot Agent retries with AI-refined selectors on failure, up to 2 retry attempts, and all returned paths are local files (ORB-safe)
</success_criteria>

<output>
After completion, create `.planning/phases/11-screenshot-quality/11-02-SUMMARY.md`
</output>
