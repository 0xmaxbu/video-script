---
phase: 02-layout-system
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/renderer/src/remotion/layouts/SplitHorizontal.tsx
  - packages/renderer/src/remotion/layouts/TextOverImage.tsx
autonomous: true
requirements:
  - VIS-05

must_haves:
  truths:
    - "SplitHorizontal uses Grid wrapper and FrostedCards for left/right split"
    - "TextOverImage uses Grid wrapper with FrostedCard for text overlay"
  artifacts:
    - path: packages/renderer/src/remotion/layouts/SplitHorizontal.tsx
      provides: Horizontal split layout refactored to use Grid + FrostedCard
      min_lines: 70
    - path: packages/renderer/src/remotion/layouts/TextOverImage.tsx
      provides: Text over image layout refactored to use Grid + FrostedCard
      min_lines: 70
  key_links:
    - from: packages/renderer/src/remotion/layouts/SplitHorizontal.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
    - from: packages/renderer/src/remotion/layouts/TextOverImage.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
---

<objective>
Refactor SplitHorizontal and TextOverImage layouts to use the Grid wrapper and FrostedCard components.

Purpose: Implement VIS-05 (layout templates) using the grid system foundation.
Output: Refactored SplitHorizontal.tsx and TextOverImage.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/renderer/src/remotion/layouts/SplitHorizontal.tsx
@packages/renderer/src/remotion/layouts/TextOverImage.tsx
@packages/renderer/src/remotion/layouts/Grid.tsx
@packages/renderer/src/remotion/layouts/FrostedCard.tsx
@packages/renderer/src/remotion/layouts/grid-utils.ts
@.planning/phases/02-layout-system/02-RESEARCH.md
</context>

<interfaces>
From packages/renderer/src/remotion/layouts/grid-utils.ts:
```typescript
export const GRID_CONSTANTS = {
  columns: 12,
  safeZone: { top: 80, right: 120, bottom: 80, left: 120 },
  gutter: 24,
  width: 1920,
  height: 1080,
} as const;

export const TYPOGRAPHY = {
  title: { hero: 80, section: 60, card: 36 },
  body: { primary: 24, secondary: 20, caption: 16 },
} as const;

export const getGridColumnPx = (col: number): number => { ... };
export const getGridSpanPx = (cols: number): number => { ... };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor SplitHorizontal to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/SplitHorizontal.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/SplitHorizontal.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor SplitHorizontal.tsx (left/right split, typically 50/50) to use Grid wrapper and FrostedCard.

**First read the existing file to understand the current structure.**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. Replace left section div with FrostedCard:
- Use 6-column span (50% of 12 columns)
- Position: left from Grid, top: 80, width via getGridSpanPx(6)
- Use TYPOGRAPHY.title.section (60pt) for any titles
- Keep existing Img rendering

4. Replace right section div with FrostedCard:
- Use 6-column span
- Position: right-aligned or using remaining columns
- Use same typography

5. Keep existing spring animations (damping: 100, stiffness: 200).
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/SplitHorizontal.tsx 2>&1 | head -20</automated>
  </verify>
  <done>SplitHorizontal uses Grid wrapper, FrostedCards for left/right split, preserved spring animations</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TextOverImage to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/TextOverImage.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/TextOverImage.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor TextOverImage.tsx to use Grid wrapper and FrostedCard for the text overlay.

**First read the existing file to understand the current structure.**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. The background image stays as-is (full-screen or large area).

4. Replace text overlay container with FrostedCard:
- Position text overlay area using Grid positioning
- Use TYPOGRAPHY.title.section (60pt) or TYPOGRAPHY.title.card (36pt) based on text role
- Body text uses TYPOGRAPHY.body.primary (24pt) or TYPOGRAPHY.body.secondary (20pt)
- Center or position the FrostedCard with text over the image

5. Keep spring animations if present.

**Key constraint:** Image is the background, FrostedCard is the overlay with text.
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/TextOverImage.tsx 2>&1 | head -20</automated>
  </verify>
  <done>TextOverImage uses Grid wrapper, FrostedCard for text overlay area, correct typography</done>
</task>

</tasks>

<verification>
- Both layouts compile with TypeScript (no errors)
- SplitHorizontal uses Grid wrapper and FrostedCards for left/right split
- TextOverImage uses Grid wrapper and FrostedCard for text overlay
- Typography uses TYPOGRAPHY constants
- Spring animations preserved
</verification>

<success_criteria>
- SplitHorizontal.tsx uses `<Grid>` wrapper and two `<FrostedCard>` components for 50/50 split
- TextOverImage.tsx uses `<Grid>` wrapper with FrostedCard for text overlay
- Typography uses correct TYPOGRAPHY values
- Both files compile without TypeScript errors
- All spring animations preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-system/02-03-SUMMARY.md`
</output>
