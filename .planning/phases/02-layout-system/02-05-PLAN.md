---
phase: 02-layout-system
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/renderer/src/remotion/layouts/BulletList.tsx
  - packages/renderer/src/remotion/layouts/Quote.tsx
autonomous: true
requirements:
  - VIS-05

must_haves:
  truths:
    - "BulletList uses Grid wrapper with FrostedCard for list container"
    - "Quote uses Grid wrapper with FrostedCard for quote styling"
  artifacts:
    - path: packages/renderer/src/remotion/layouts/BulletList.tsx
      provides: Bullet list layout refactored to use Grid + FrostedCard
      min_lines: 70
    - path: packages/renderer/src/remotion/layouts/Quote.tsx
      provides: Quote layout refactored to use Grid + FrostedCard
      min_lines: 70
  key_links:
    - from: packages/renderer/src/remotion/layouts/BulletList.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
    - from: packages/renderer/src/remotion/layouts/Quote.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
---

<objective>
Refactor BulletList and Quote layouts to use the Grid wrapper and FrostedCard components.

Purpose: Implement VIS-05 (layout templates) using the grid system foundation.
Output: Refactored BulletList.tsx and Quote.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/renderer/src/remotion/layouts/BulletList.tsx
@packages/renderer/src/remotion/layouts/Quote.tsx
@packages/renderer/src/remotion/layouts/Grid.tsx
@packages/renderer/src/remotion/layouts/FrostedCard.tsx
@packages/renderer/src/remotion/layouts/grid-utils.ts
@.planning/phases/02-layout-system/02-RESEARCH.md
</context>

<interfaces>
From packages/renderer/src/remotion/layouts/grid-utils.ts:
```typescript
export const GRID_CONSTANTS = {
  columns: 12,
  safeZone: { top: 80, right: 120, bottom: 80, left: 120 },
  gutter: 24,
  width: 1920,
  height: 1080,
} as const;

export const TYPOGRAPHY = {
  title: { hero: 80, section: 60, card: 36 },
  body: { primary: 24, secondary: 20, caption: 16 },
} as const;

export const getGridColumnPx = (col: number): number => { ... };
export const getGridSpanPx = (cols: number): number => { ... };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor BulletList to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/BulletList.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/BulletList.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor BulletList.tsx to use Grid wrapper and FrostedCard for the list container.

**First read the existing file to understand the current structure (title, bullet items).**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. List container:
- Wrap the bullet list area in FrostedCard
- Use TYPOGRAPHY.title.section (60pt) for list title
- Use TYPOGRAPHY.body.primary (24pt) for bullet item text
- Bullets can be styled with custom rendering or emoji

4. Keep spring animations for list item appear effects if present.

**Key constraint:** Body text at 24pt for readability of bullet items.
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/BulletList.tsx 2>&1 | head -20</automated>
  </verify>
  <done>BulletList uses Grid wrapper, FrostedCard for list container, 24pt body text</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Quote to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/Quote.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/Quote.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor Quote.tsx to use Grid wrapper and FrostedCard for the quote styling.

**First read the existing file to understand the current structure (large quote mark, quote text, attribution).**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. Quote container:
- Wrap the quote area in FrostedCard
- Large decorative quote mark can overflow or be positioned absolutely
- Quote text: TYPOGRAPHY.title.section (60pt) or larger for impact
- Attribution: TYPOGRAPHY.body.secondary (20pt)
- Center the FrostedCard for visual impact

4. The FrostedCard provides the "center frosted glass card" effect per the design notes.

**Key constraint:** Quote should feel impactful, use larger typography. FrostedCard should be centered.

**Note:** Per 02-RESEARCH.md open question 3, Quote may need special handling for decorative elements. Keep the large quote mark styling but ensure it works within the Grid context.
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/Quote.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Quote uses Grid wrapper, FrostedCard for centered quote, decorative quote mark preserved</done>
</task>

</tasks>

<verification>
- Both layouts compile with TypeScript (no errors)
- BulletList uses Grid wrapper and FrostedCard for list container
- Quote uses Grid wrapper and FrostedCard for centered quote styling
- Typography uses TYPOGRAPHY constants (60pt titles, 24pt/20pt body)
- Spring animations preserved if present
</verification>

<success_criteria>
- BulletList.tsx uses `<Grid>` wrapper and FrostedCard for list container
- BulletList title uses 60pt, bullet text uses 24pt
- Quote.tsx uses `<Grid>` wrapper and centered FrostedCard for quote
- Quote has decorative quote mark styling preserved
- Both files compile without TypeScript errors
- All spring animations preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-system/02-05-SUMMARY.md`
</output>
