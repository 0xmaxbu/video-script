---
phase: 02-layout-system
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
  - packages/renderer/src/remotion/layouts/SplitVertical.tsx
autonomous: true
requirements:
  - VIS-05

must_haves:
  truths:
    - "HeroFullscreen uses Grid wrapper and FrostedCard for title area"
    - "SplitVertical uses Grid wrapper with 60/40 split using FrostedCard"
  artifacts:
    - path: packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
      provides: Hero layout refactored to use Grid + FrostedCard
      min_lines: 70
    - path: packages/renderer/src/remotion/layouts/SplitVertical.tsx
      provides: Vertical split layout refactored to use Grid + FrostedCard
      min_lines: 80
  key_links:
    - from: packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
    - from: packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
      to: packages/renderer/src/remotion/layouts/FrostedCard.tsx
      via: imports FrostedCard
    - from: packages/renderer/src/remotion/layouts/SplitVertical.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
    - from: packages/renderer/src/remotion/layouts/SplitVertical.tsx
      to: packages/renderer/src/remotion/layouts/FrostedCard.tsx
      via: imports FrostedCard
---

<objective>
Refactor HeroFullscreen and SplitVertical layouts to use the Grid wrapper and FrostedCard components.

Purpose: Implement VIS-05 (layout templates) using the grid system foundation.
Output: Refactored HeroFullscreen.tsx and SplitVertical.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
@packages/renderer/src/remotion/layouts/SplitVertical.tsx
@packages/renderer/src/remotion/layouts/Grid.tsx
@packages/renderer/src/remotion/layouts/FrostedCard.tsx
@packages/renderer/src/remotion/layouts/grid-utils.ts
@.planning/phases/02-layout-system/02-RESEARCH.md
</context>

<interfaces>
From packages/renderer/src/remotion/layouts/grid-utils.ts:
```typescript
export const GRID_CONSTANTS = {
  columns: 12,
  safeZone: { top: 80, right: 120, bottom: 80, left: 120 },
  gutter: 24,
  width: 1920,
  height: 1080,
} as const;

export const TYPOGRAPHY = {
  title: { hero: 80, section: 60, card: 36 },
  body: { primary: 24, secondary: 20, caption: 16 },
} as const;

export const getGridColumnPx = (col: number): number => { ... };
export const getGridSpanPx = (cols: number): number => { ... };
export const getGridColumnPct = (col: number): number => { ... };
export const getGridSpanPct = (cols: number): number => { ... };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HeroFullscreen to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/HeroFullscreen.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/HeroFullscreen.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor HeroFullscreen.tsx to use Grid wrapper and FrostedCard for the title area.

**Changes required:**

1. Add imports for Grid, FrostedCard, and TYPOGRAPHY:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY } from "./grid-utils.js";
```

2. Wrap the entire layout in `<Grid>`:
```tsx
return (
  <Grid>
    {/* existing content */}
  </Grid>
);
```

3. Replace the title container with FrostedCard:
- Current: Uses AbsoluteFill with manual paddingBottom: "10%"
- Replace with: FrostedCard positioned at bottom with opacity animation
- Use TYPOGRAPHY.title.hero (80pt) for title fontSize
- Use TYPOGRAPHY.body.secondary (20pt) for subtitle fontSize

4. Keep all existing spring animations and interpolate logic unchanged.

**Key constraints:**
- Keep background Img and gradient overlay as-is (they are the full-screen background)
- FrostedCard is for the title area only
- Use spring damping: 100, stiffness: 200 for animations (per existing pattern)
- Title Y translation: interpolate from [-50, 0] (not [-30, 0])

Verify typography: hero title must be 80pt (TYPOGRAPHY.title.hero).
  </action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/HeroFullscreen.tsx 2>&1 | head -20</automated>
  </verify>
  <done>HeroFullscreen uses Grid wrapper, FrostedCard for title, 80pt hero typography, and existing spring animations</done>
</task>

<task type="auto">
  <name>Task 2: Refactor SplitVertical to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/SplitVertical.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/SplitVertical.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor SplitVertical.tsx to use Grid wrapper and FrostedCard components.

**Changes required:**

1. Add imports for Grid, FrostedCard, and TYPOGRAPHY:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY } from "./grid-utils.js";
```

2. Wrap the layout in `<Grid>`:
```tsx
return (
  <Grid>
    {/* existing content */}
  </Grid>
);
```

3. Replace upper section (60%) div with FrostedCard:
- Current: `<div style={{ height: "60%", ... }}>`
- Replace with: FrostedCard with height "calc(60% - 80px)" accounting for top safe zone
- Position: top: 80, left: 120, width: 100% - safe zones (use getGridSpanPx(12) or percentage)
- Keep existing Img rendering inside FrostedCard
- Use color="rgba(0,0,0," for dark theme (dark content area)

4. Replace lower section (40%) div with FrostedCard:
- Current: `<div style={{ height: "40%", backgroundColor: "#1a1a1a", ... }}>`
- Replace with: FrostedCard with height "calc(40% - 80px)"
- Position: bottom: 80, left: 120, width: 100% - safe zones
- Use color="rgba(0,0,0," for dark theme
- Keep borderTop visual as border style in FrostedCard

5. Typography:
- Title: TYPOGRAPHY.title.section (60pt)
- Keep existing Img and title rendering

**Key constraints:**
- Spring animations unchanged (damping: 100, stiffness: 200)
- Split stays 60/40 ratio
- FrostedCards replace the div wrappers with same layout behavior
  </action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/SplitVertical.tsx 2>&1 | head -20</automated>
  </verify>
  <done>SplitVertical uses Grid wrapper, FrostedCards for 60/40 split areas, 60pt section typography</done>
</task>

</tasks>

<verification>
- Both layouts compile with TypeScript (no errors)
- HeroFullscreen imports and uses Grid, FrostedCard, TYPOGRAPHY
- SplitVertical imports and uses Grid, FrostedCard, TYPOGRAPHY
- Spring animations preserved with damping: 100, stiffness: 200
- Typography uses TYPOGRAPHY constants (80pt hero, 60pt section)
</verification>

<success_criteria>
- HeroFullscreen.tsx uses `<Grid>` wrapper and `<FrostedCard>` for title area
- HeroFullscreen title uses TYPOGRAPHY.title.hero (80pt)
- SplitVertical.tsx uses `<Grid>` wrapper and two `<FrostedCard>` components (60/40)
- SplitVertical title uses TYPOGRAPHY.title.section (60pt)
- Both files compile without TypeScript errors
- All spring animations preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-system/02-02-SUMMARY.md`
</output>
