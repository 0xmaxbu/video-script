---
phase: 02-layout-system
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/renderer/src/remotion/layouts/CodeFocus.tsx
  - packages/renderer/src/remotion/layouts/Comparison.tsx
autonomous: true
requirements:
  - VIS-05

must_haves:
  truths:
    - "CodeFocus uses Grid wrapper with FrostedCard for code display area"
    - "Comparison uses Grid wrapper with FrostedCards for VS elements"
  artifacts:
    - path: packages/renderer/src/remotion/layouts/CodeFocus.tsx
      provides: Code focus layout refactored to use Grid + FrostedCard
      min_lines: 70
    - path: packages/renderer/src/remotion/layouts/Comparison.tsx
      provides: Comparison layout refactored to use Grid + FrostedCard
      min_lines: 80
  key_links:
    - from: packages/renderer/src/remotion/layouts/CodeFocus.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
    - from: packages/renderer/src/remotion/layouts/Comparison.tsx
      to: packages/renderer/src/remotion/layouts/Grid.tsx
      via: imports Grid
---

<objective>
Refactor CodeFocus and Comparison layouts to use the Grid wrapper and FrostedCard components.

Purpose: Implement VIS-05 (layout templates) using the grid system foundation.
Output: Refactored CodeFocus.tsx and Comparison.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/renderer/src/remotion/layouts/CodeFocus.tsx
@packages/renderer/src/remotion/layouts/Comparison.tsx
@packages/renderer/src/remotion/layouts/Grid.tsx
@packages/renderer/src/remotion/layouts/FrostedCard.tsx
@packages/renderer/src/remotion/layouts/grid-utils.ts
@.planning/phases/02-layout-system/02-RESEARCH.md
</context>

<interfaces>
From packages/renderer/src/remotion/layouts/grid-utils.ts:
```typescript
export const GRID_CONSTANTS = {
  columns: 12,
  safeZone: { top: 80, right: 120, bottom: 80, left: 120 },
  gutter: 24,
  width: 1920,
  height: 1080,
} as const;

export const TYPOGRAPHY = {
  title: { hero: 80, section: 60, card: 36 },
  body: { primary: 24, secondary: 20, caption: 16 },
} as const;

export const getGridColumnPx = (col: number): number => { ... };
export const getGridSpanPx = (cols: number): number => { ... };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CodeFocus to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/CodeFocus.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/CodeFocus.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor CodeFocus.tsx to use Grid wrapper and FrostedCard for the code display area.

**First read the existing file to understand the current structure.**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. Code display area:
- If there's a container div around the code, replace with FrostedCard
- Use TYPOGRAPHY.body.caption (16pt) for code text (code is typically smaller)
- Keep any title using TYPOGRAPHY.title.section (60pt) or TYPOGRAPHY.title.card (36pt)
- CodeFocus typically has code as the primary content with minimal text overlay

4. Keep any spring animations unchanged.

**Key constraint:** Code text typically stays at smaller sizes (16pt caption), do not enlarge code itself.
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/CodeFocus.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CodeFocus uses Grid wrapper, FrostedCard for code area, preserved animations</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Comparison to use Grid and FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/Comparison.tsx</files>
  <read_first>
    packages/renderer/src/remotion/layouts/Comparison.tsx
    packages/renderer/src/remotion/layouts/Grid.tsx
    packages/renderer/src/remotion/layouts/FrostedCard.tsx
    packages/renderer/src/remotion/layouts/grid-utils.ts
  </read_first>
  <action>
Refactor Comparison.tsx (typically "A vs B" layout) to use Grid wrapper and FrostedCard components.

**First read the existing file to understand the current structure (VS element handling, left/right panels).**

**Changes required:**

1. Add imports:
```typescript
import { Grid } from "./Grid.js";
import { FrostedCard } from "./FrostedCard.js";
import { TYPOGRAPHY, getGridSpanPx } from "./grid-utils.js";
```

2. Wrap content in `<Grid>`.

3. Left panel (Option A):
- Wrap in FrostedCard spanning ~5-6 columns
- Use TYPOGRAPHY.title.card (36pt) for option titles
- Keep Img rendering for screenshots

4. Right panel (Option B):
- Wrap in FrostedCard spanning ~5-6 columns
- Same typography as left panel

5. VS element:
- Position using Grid (center columns or absolute positioned text)
- Use TYPOGRAPHY.title.section (60pt) for "VS" text
- Can use FrostedCard or plain text styling for VS badge

6. Keep spring animations if present.

**Key constraint:** VS element should stand out, use large typography (60pt).
</action>
  <verify>
    <automated>cd /Volumes/SN350-1T\ 1/dev/video-script && npx tsc --noEmit packages/renderer/src/remotion/layouts/Comparison.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Comparison uses Grid wrapper, FrostedCards for A/B panels, VS badge with 60pt typography</done>
</task>

</tasks>

<verification>
- Both layouts compile with TypeScript (no errors)
- CodeFocus uses Grid wrapper and FrostedCard for code area
- Comparison uses Grid wrapper and FrostedCards for left/right panels + VS element
- Typography uses TYPOGRAPHY constants
- Spring animations preserved
</verification>

<success_criteria>
- CodeFocus.tsx uses `<Grid>` wrapper and FrostedCard for code display
- Comparison.tsx uses `<Grid>` wrapper and FrostedCards for A/B panels
- Comparison has VS badge with 60pt typography
- Both files compile without TypeScript errors
- All spring animations preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-system/02-04-SUMMARY.md`
</output>
