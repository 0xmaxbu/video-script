---
phase: 03-research-content
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/mastra/agents/script-agent.ts
  - src/mastra/agents/research-agent.ts
  - src/mastra/pipelines/research-pipeline.ts
autonomous: true
requirements:
  - RES-03
  - SCR-01
  - SCR-02

must_haves:
  truths:
    - "Script generates tutorial-friendly narration with depth"
    - "Scene narration is engaging, not generic summaries"
    - "Research extracts semantic chunks with relationship tags"
    - "Script agent outputs NewSceneSchema compatible with visual layer"
    - "Each topic generates independent research + script (D-08)"
  artifacts:
    - path: "src/mastra/agents/script-agent.ts"
      provides: "Tutorial-friendly script generation with scene segmentation"
      contains: "tutorial-friendly", "NewSceneSchema", "narration"
    - path: "src/mastra/agents/research-agent.ts"
      provides: "Research with relationship tags for narrative logic"
      contains: "relationship", "原因", "对比", "示例"
    - path: "src/mastra/pipelines/research-pipeline.ts"
      provides: "Topic-splitting pipeline (D-08)"
      contains: "for.*links", "独立", "topicResults"
  key_links:
    - from: "src/mastra/agents/research-agent.ts"
      to: "src/mastra/agents/script-agent.ts"
      via: "research markdown output with relationship tags"
      pattern: "ResearchOutput.*relationship"
---

<objective>
Update research agent to output relationship tags (D-02) and update script agent instructions for tutorial-friendly depth narration with content-driven scene distribution (D-03, D-04). Implement topic splitting in research-pipeline.ts per D-08.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/mastra/agents/research-agent.ts
@src/mastra/agents/script-agent.ts
@src/types/visual.ts
@src/types/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update research-agent.ts with relationship tags</name>
  <files>src/mastra/agents/research-agent.ts</files>
  <read_first>
    src/mastra/agents/research-agent.ts
    src/types/research.ts
  </read_first>
  <action>
Update the research agent instructions to include relationship tag output per D-02.

**Add to the OUTPUT FORMAT section in the agent instructions:**

After the existing priority tag guidelines, add:

```
## RELATIONSHIP TAGS (D-02):

Mark the relationship between chunks using these tags:
- [relationship: 原因] - Why something works or matters (cause/reason)
- [relationship: 对比] - Comparing alternatives or before/after
- [relationship: 示例] - Concrete code or use case example
- [relationship: 注意事项] - Warnings or important notes

A chunk can have multiple relationship tags. Example:
### 1. NoInfer 工具类型 [priority: important][relationship: 示例]

`NoInfer<T>` 用于阻止类型推断...

[relationship: 原因] 解决闭包中类型收窄丢失的问题
```

**Update the example output** to show relationship tags in action. The example should demonstrate:
1. A feature section with [relationship: 示例] showing code example
2. A section with [relationship: 原因] explaining why it matters
3. A section with [relationship: 注意事项] for warnings

**CRITICAL:** The output format must still use Markdown but include relationship tags. The relationship tags help the script agent reconstruct narrative logic.

**Acceptance criteria:**
- Agent instructions include relationship tag definitions
- Example output demonstrates relationship tags in use
- Relationship tags are: 原因, 对比, 示例, 注意事项
- Multiple tags per chunk are allowed
  </action>
  <verify>
    <automated>cd "/Volumes/SN350-1T 1/dev/video-script" && grep -q "relationship: 原因" src/mastra/agents/research-agent.ts && grep -q "relationship: 对比" src/mastra/agents/research-agent.ts && grep -q "relationship: 示例" src/mastra/agents/research-agent.ts && grep -q "relationship: 注意事项" src/mastra/agents/research-agent.ts</automated>
  </verify>
  <done>Research agent outputs relationship tags (原因/对比/示例/注意事项) per D-02</done>
</task>

<task type="auto">
  <name>Task 2: Update script-agent.ts for tutorial-friendly depth narration</name>
  <files>src/mastra/agents/script-agent.ts</files>
  <read_first>
    src/mastra/agents/script-agent.ts
    src/types/visual.ts
  </read_first>
  <action>
Update the script agent instructions to implement D-03 (tutorial-friendly tone) and D-04 (content-driven scene distribution).

**Key instruction updates:**

1. **Narration tone (D-03):** Replace existing tone guidance with:
```
**Narration tone = 教程友好 (Tutorial-Friendly)**
- 讲解型，像老师上课
- Explain WHY things work, not just WHAT they are
- Use analogies and concrete examples
- Anticipate viewer questions and address them
- Make complex concepts approachable
```

2. **Scene distribution (D-04):** Add content-driven scene count:
```
## Scene Distribution (D-04):
- Agent auto-assigns scene count based on content complexity
- 5-10 scenes typical for 10-15 minute video
- Scene types: intro (1), feature (N), code (N), outro (1)
- CLI --duration flag overrides automatic calculation

**Content elasticity (D-06):**
- Too little content: Add concrete code examples to demonstrate
- Too much content: Compress supporting-level content
- Essential/Important content always retained
```

3. **Output schema:** Confirm output uses NewSceneSchema from visual.ts:
```typescript
// Scene output must match NewSceneSchema
{
  "id": "scene-1",
  "type": "intro" | "feature" | "code" | "outro",
  "title": "场景标题",
  "duration": 30, // seconds
  "narration": {
    "fullText": "完整口播文案",
    "estimatedDuration": 30,
    "segments": [
      { "text": "第一段", "startTime": 0, "endTime": 3 }
    ]
  },
  "highlights": [...],
  "codeHighlights": [...],
  "sourceRef": "[1]"
}
```

4. **Engaging narration (SCR-02):** Replace generic summary guidance with:
```
**Engaging narration (SCR-02):**
- NOT: "This feature has three benefits..."
- INSTEAD: "Imagine you're working on a large codebase. Without NoInfer, TypeScript might guess wrong here. But with NoInfer..."
- Make viewers feel the problem before showing the solution
- Build anticipation for key insights
```

**Acceptance criteria:**
- Script agent instructions mention "tutorial-friendly" tone
- Instructions explain content-driven scene distribution
- Instructions address content elasticity (D-06)
- Output schema is NewSceneSchema compatible
- Narration guidance emphasizes explaining WHY, not just WHAT
  </action>
  <verify>
    <automated>cd "/Volumes/SN350-1T 1/dev/video-script" && grep -q "tutorial-friendly" src/mastra/agents/script-agent.ts && grep -q "content-driven" src/mastra/agents/script-agent.ts && grep -q "NewSceneSchema" src/mastra/agents/script-agent.ts</automated>
  </verify>
  <done>Script agent updated for tutorial-friendly depth narration and content-driven scene distribution per D-03, D-04, D-06</done>
</task>

<task type="auto">
  <name>Task 3: Implement topic splitting in research-pipeline.ts (D-08)</name>
  <files>src/mastra/pipelines/research-pipeline.ts</files>
  <read_first>
    src/mastra/tools/web-fetch.ts
    src/mastra/tools/validators/input-validator.ts
    src/mastra/agents/research-agent.ts
    src/mastra/agents/quality-agent.ts
  </read_first>
  <action>
Create `src/mastra/pipelines/research-pipeline.ts` orchestrating the complete research flow with topic splitting per D-08.

**D-08 Requirement:** Multi-topic handling - each topic generates independent research + script. Iterate over input.links and process each topic independently.

**Pipeline implementation:**
```typescript
import { webFetchTool } from "../tools/web-fetch.js";
import { validateFetchedContent } from "../tools/validators/input-validator.js";
import { researchAgent } from "../agents/research-agent.js";
import { evaluateQualityAsync } from "../agents/quality-agent.js";
import { withRetry } from "../../utils/retry.js";

export interface TopicInput {
  url: string;
  key: string;
}

export interface ResearchPipelineInput {
  title: string;
  links: Array<TopicInput>;
  document?: string;
}

export interface TopicResult {
  topicKey: string;
  url: string;
  researchMarkdown: string;
  validationFailures?: Array<{ name: string; reason: string }>;
}

export interface ResearchPipelineOutput {
  title: string;
  topicResults: Array<TopicResult>;
  totalTopics: number;
}

/**
 * Complete research pipeline with TOPIC SPLITTING (D-08)
 * Each link/topic is processed independently with its own research + quality evaluation
 * Per D-08: 多 topic 分开处理 - 每个 topic 独立生成研究 + 脚本
 *
 * Per D-09: 3 retries on failure, then fail with specific error
 * Per D-10: Input validation before passing to agent
 * Per D-11: Quality evaluation runs async, does not block
 */
export async function runResearchPipeline(
  input: ResearchPipelineInput,
  options: { maxRetries?: number } = {}
): Promise<ResearchPipelineOutput> {
  const maxRetries = options.maxRetries ?? 3;
  const retryOptions = { maxRetries, initialDelayMs: 1000, maxDelayMs: 5000, factor: 2 };

  // D-08: Process each topic independently
  const topicResults: Array<TopicResult> = [];

  for (const link of input.links) {
    // Step 1: Fetch this topic's content
    const fetchResult = await withRetry(
      async () => {
        const result = await webFetchTool.execute({ url: link.url });
        return result;
      },
      retryOptions
    );

    // Step 2: Validate fetched content (D-10)
    const validation = validateFetchedContent(fetchResult.content, fetchResult.url);

    let validationFailures: Array<{ name: string; reason: string }> | undefined;
    if (!validation.valid) {
      validationFailures = validation.failures;
      if (maxRetries > 0) {
        // Retry logic handled by withRetry above, but if still invalid, record failures
        console.warn(`[Research Pipeline] Validation warnings for ${link.url}:`, validation.failures);
      }
    }

    // Step 3: Run research agent for this topic (independent research per D-08)
    const researchResult = await researchAgent.run(
      `Title: ${link.key}\n\nURL: ${link.url}\n\nContent:\n${fetchResult.title}\n${fetchResult.content}`
    );

    const researchMarkdown = researchResult.text();

    // Step 4: Quality evaluation for this topic (non-blocking per D-11)
    evaluateQualityAsync(
      researchMarkdown,
      (score) => {
        console.log(`[Research Pipeline] Quality score for "${link.key}": ${score.qualityScore}`);
      },
      (error) => {
        console.warn(`[Research Pipeline] Quality evaluation failed for "${link.key}": ${error.message}`);
      }
    );

    topicResults.push({
      topicKey: link.key,
      url: link.url,
      researchMarkdown,
      validationFailures,
    });
  }

  return {
    title: input.title,
    topicResults,
    totalTopics: topicResults.length,
  };
}
```

**Key D-08 implementation points:**
- `for (const link of input.links)` - iterate over each topic independently
- Each topic gets its own: fetch -> validate -> research -> quality evaluation
- `topicResults` array collects independent results per topic
- Topics are NOT batched together - each is processed separately per D-08

**Key orchestration points:**
- D-09: Uses withRetry for fetch failures (3 retries)
- D-10: Validates content before research agent
- D-11: Quality evaluation runs async per topic, does not block
- Sequential per-topic: fetch -> validate -> research -> async quality

**Acceptance criteria:**
- Pipeline iterates over input.links with `for` loop (D-08)
- Each topic processed independently with separate researchMarkdown
- withRetry handles fetch failures (3 retries per D-09)
- validateFetchedContent called before research agent per topic
- Quality evaluation is fire-and-forget per topic (does not block)
- Returns array of TopicResult with researchMarkdown per topic
  </action>
  <verify>
    <automated>cd "/Volumes/SN350-1T 1/dev/video-script" && test -f src/mastra/pipelines/research-pipeline.ts && grep -q "for.*const link of input.links" src/mastra/pipelines/research-pipeline.ts && grep -q "topicResults" src/mastra/pipelines/research-pipeline.ts && grep -q "TopicResult" src/mastra/pipelines/research-pipeline.ts</automated>
  </verify>
  <done>research-pipeline.ts implements topic splitting (D-08) - each link processed independently with separate research + quality evaluation</done>
</task>

</tasks>

<verification>
- research-agent.ts includes relationship tag instructions (原因/对比/示例/注意事项)
- script-agent.ts mentions tutorial-friendly tone and content-driven scene distribution
- script-agent.ts output matches NewSceneSchema from visual.ts
- research-pipeline.ts exists and implements topic splitting with `for (const link of input.links)`
- Each topic gets independent researchMarkdown output
</verification>

<success_criteria>
- Research agent outputs relationship tags per D-02
- Script agent generates tutorial-friendly narration with depth per D-03
- Script agent uses content-driven scene distribution per D-04
- Pipeline implements D-08 topic splitting - each topic processed independently
- Pipeline validates input per D-10 before research
- Quality evaluation runs async per D-11 without blocking
- All outputs compatible with visual layer (NewSceneSchema)
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-content/03-03-SUMMARY.md`
</output>
