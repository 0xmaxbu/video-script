---
phase: 12-fix-layout-rendering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/renderer/src/remotion/layouts/FrostedCard.tsx
  - packages/renderer/src/remotion/layouts/SplitVertical.tsx
autonomous: true
requirements:
  - VIS-04
  - VIS-05
user_setup: []
must_haves:
  truths:
    - "FrostedCard renders with configurable z-index"
    - "Text content appears above frosted glass (not blurred)"
    - "SplitVertical uses flexbox centering instead of absolute positioning"
    - "Content is vertically and horizontally centered in layout sections"
  artifacts:
    - path: "packages/renderer/src/remotion/layouts/FrostedCard.tsx"
      provides: "Frosted glass component with z-index control"
      contains: "zIndex"
    - path: "packages/renderer/src/remotion/layouts/SplitVertical.tsx"
      provides: "Split vertical layout with proper centering"
      min_lines: 80
  key_links:
    - from: "FrostedCard.tsx"
      to: "SplitVertical.tsx"
      via: "FrostedCard component usage"
      pattern: "FrostedCard.*zIndex"
---

<objective>
Fix Z-index stacking and vertical centering in layout components.

Purpose: Ensure frosted glass appears behind text content and layouts use proper flexbox centering.
Output: Fixed FrostedCard with zIndex prop, SplitVertical with flexbox centering.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-layout-rendering/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zIndex prop to FrostedCard</name>
  <files>packages/renderer/src/remotion/layouts/FrostedCard.tsx</files>
  
  <read_first>
    - packages/renderer/src/remotion/layouts/FrostedCard.tsx (current implementation)
    - packages/renderer/src/remotion/layouts/SplitVertical.tsx (consumer of FrostedCard)
  </read_first>
  
  <action>
    Per D-01: FrostedCard z-index: 10, Text content z-index: 20
    
    Modify `packages/renderer/src/remotion/layouts/FrostedCard.tsx`:
    
    1. Add `zIndex?: number` to FrostedCardProps interface (after `color` prop)
    2. Add default value: `zIndex = 10` in destructured props
    3. Add `zIndex` to the wrapper div's style object
    
    The interface should become:
    ```typescript
    interface FrostedCardProps {
      children: ReactNode;
      style?: React.CSSProperties;
      opacity?: number; // default 0.2 (20%)
      blur?: number; // default 25
      radius?: number; // default 32
      color?: string; // default "rgba(255,255,255,"
      zIndex?: number; // default 10 (NEW - per D-01)
    }
    ```
    
    The style object should include:
    ```typescript
    zIndex: zIndex,
    ```
    
    Do NOT change any other functionality.
  </action>
  
  <verify>
    <automated>grep -n "zIndex" packages/renderer/src/remotion/layouts/FrostedCard.tsx | head -5</automated>
  </verify>
  
  <acceptance_criteria>
    - FrostedCard.tsx contains `zIndex?: number` in interface
    - FrostedCard.tsx contains `zIndex = 10` in default props
    - FrostedCard.tsx contains `zIndex: zIndex` in style object
  </acceptance_criteria>
  
  <done>FrostedCard has configurable zIndex prop with default 10, text content can render above glass with zIndex: 20</done>
</task>

<task type="auto">
  <name>Task 2: Update SplitVertical to use flexbox centering</name>
  <files>packages/renderer/src/remotion/layouts/SplitVertical.tsx</files>
  
  <read_first>
    - packages/renderer/src/remotion/layouts/SplitVertical.tsx (current implementation)
    - packages/renderer/src/remotion/layouts/Comparison.tsx (reference for flexbox pattern)
  </read_first>
  
  <action>
    Per D-03: Use Flexbox with alignItems:center and justifyContent:center for content within layout sections
    
    Modify `packages/renderer/src/remotion/layouts/SplitVertical.tsx`:
    
    1. **Top section FrostedCard** - Replace absolute positioning with flexbox:
       - Remove: `position: "absolute"`, `top: 80`
       - Keep: `display: "flex"`, `alignItems: "center"`, `justifyContent: "center"`
       - Keep: width and height calculations
       
    2. **Bottom section FrostedCard** - Replace absolute positioning with flexbox:
       - Remove: `position: "absolute"`, `bottom: 80`
       - Keep: `display: "flex"`, `flexDirection: "column"`, `alignItems: "center"`, `justifyContent: "center"`
       - Keep: width and height calculations
    
    3. **Wrap both sections in a flex container** inside Grid:
       ```tsx
       <Grid>
         <div style={{
           display: "flex",
           flexDirection: "column",
           height: "100%",
           padding: "80px 120px",
           gap: "1rem",
         }}>
           {/* Top section - 60% */}
           <FrostedCard style={{ flex: 6, ... }}>
             ...
           </FrostedCard>
           
           {/* Bottom section - 40% */}
           <FrostedCard style={{ flex: 4, ... }}>
             ...
           </FrostedCard>
         </div>
       </Grid>
       ```
    
    4. Ensure FrostedCard zIndex is set to 10 (default) and text content inside has implicit higher z-index
    
    Do NOT change the animation logic (spring, interpolate) or content rendering logic.
  </action>
  
  <verify>
    <automated>grep -c "position.*absolute" packages/renderer/src/remotion/layouts/SplitVertical.tsx || echo "0"</automated>
  </verify>
  
  <acceptance_criteria>
    - SplitVertical.tsx does NOT contain `position: "absolute"` on FrostedCard elements
    - SplitVertical.tsx does NOT contain `top: 80` or `bottom: 80` on FrostedCard elements
    - SplitVertical.tsx uses flex container with `display: "flex"` and `flexDirection: "column"`
    - SplitVertical.tsx uses `flex: 6` for top section (60%)
    - SplitVertical.tsx uses `flex: 4` for bottom section (40%)
  </acceptance_criteria>
  
  <done>SplitVertical uses flexbox centering, content is properly centered vertically and horizontally</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. FrostedCard has zIndex prop with default 10
2. SplitVertical uses flexbox layout instead of absolute positioning
3. TypeScript compiles without errors: `pnpm tsc --noEmit`
</verification>

<success_criteria>

- FrostedCard.tsx has zIndex?: number prop with default 10
- SplitVertical.tsx uses flex container with flex: 6/4 ratio
- No absolute positioning on FrostedCard elements in SplitVertical
- TypeScript compilation passes
  </success_criteria>

<output>
After completion, create `.planning/phases/12-fix-layout-rendering/12-01-SUMMARY.md`
</output>
