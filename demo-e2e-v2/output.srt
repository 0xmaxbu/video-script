1
00:00:00,000 --> 00:00:15,000
欢迎观看本视频！React Hooks是现代React开发的核心功能，它彻底改变了我们编写组件的方式。今天我们将系统学习所有常用的React内置Hooks，以及如何创建自定义Hooks来实现代码复用。

2
00:00:15,000 --> 00:00:40,000
Hooks是React提供的特殊函数，它们让你在函数组件中使用各种React特性。在Hooks出现之前，我们需要使用class组件才能使用state和生命周期等功能。现在，通过组合内置Hooks或构建自定义Hooks，我们可以实现模块化和代码复用，让组件逻辑更加清晰和可维护。

3
00:00:40,000 --> 00:01:10,000
State Hooks让组件能够记住信息，比如用户输入的内容或者选中状态。useState是最基础的state hook，用于直接更新状态变量，适合简单的状态管理。而useReducer将状态逻辑封装在reducer函数中，适合处理复杂的状态转换逻辑，比如有多个相关状态或者状态更新有规律的场景。

4
00:01:10,000 --> 00:02:00,000
让我们看一些实际的代码示例。首先是useState的典型用法：我们声明一个状态变量和它的更新函数，然后在按钮点击时更新状态。接着是useReducer的例子：定义一个reducer函数来处理不同的action类型，通过dispatch来触发状态更新。选择useState还是useReducer取决于状态管理的复杂程度。

5
00:02:00,000 --> 00:02:25,000
Context Hook也就是useContext，它允许组件从遥远的父组件接收信息，而无需通过props层层传递。这解决了React中著名的props drilling问题。比如在一个大型应用中，顶部组件定义的主题配置或者用户信息，可以通过Context直接传递给深层组件，而不需要每一层都手动传递。

6
00:02:25,000 --> 00:03:05,000
使用Context需要三个步骤：首先是创建Context对象，然后用Provider包裹子组件并传入value值，最后在任何子组件中使用useContext获取这个值。这样无论组件层级有多深，都能直接获取到顶层传入的数据，而不需要手动层层传递props。

7
00:03:05,000 --> 00:03:30,000
Ref Hooks包括useRef和useImperativeHandle，它们让组件持有不用于渲染的信息。比如存储DOM节点引用来直接操作元素，或者保存setTimeout返回的ID用于清除定时器。与state不同，更新ref不会触发组件重新渲染。ref是React连接外部世界的桥梁。

8
00:03:30,000 --> 00:04:10,000
useRef最常见的用法是获取DOM元素的引用。我们创建一个ref对象，将它绑定到input元素的ref属性上，然后在需要时通过ref.current访问DOM节点，比如聚焦输入框。另外useRef也可以用来存储不需要触发渲染的值，比如定时器ID。useImperativeHandle则让你自定义暴露给父组件的实例值。

9
00:04:10,000 --> 00:04:50,000
Effect Hooks让组件连接和同步外部系统，包括网络请求、浏览器API、动画等。useEffect是最常用的effect hook，它在每次渲染后根据依赖数组决定是否执行。useLayoutEffect在浏览器重绘前同步触发，适合需要立即读取或修改DOM的场景。useInsertionEffect则在DOM变更前触发，专供CSS-in-JS库使用。

10
00:04:50,000 --> 00:05:40,000
看几个useEffect的实际例子。首先是基础用法：指定依赖数组为空时只在挂载时执行一次，相当于class组件的componentDidMount。返回的清理函数会在组件卸载时执行，用于清除定时器或取消订阅。然后我们对比三种effect的区别，useInsertionEffect最早执行用于注入样式，useLayoutEffect在DOM修改后同步执行，useEffect则在渲染后异步执行。

11
00:05:40,000 --> 00:06:10,000
Performance Hooks用于优化重渲染性能。useMemo用于缓存昂贵计算的结果，避免每次渲染都重新计算。useCallback缓存函数定义，配合子组件的memo使用可以防止不必要的重渲染。useTransition将状态转换标记为非阻塞，让紧急的UI更新能优先处理。useDeferredValue则延迟更新非关键UI部分。

12
00:06:10,000 --> 00:06:40,000
来看这些优化hook的用法。useMemo包裹一个计算逻辑，只有当依赖变化时才重新计算。useCallback则是缓存函数引用，配合React.memo可以让子组件知道函数是否真的变化了。useTransition用于标记非紧急更新，比如搜索输入时可以让列表更新不阻塞输入框响应。useDeferredValue类似但作用于单个值。

13
00:06:40,000 --> 00:07:00,000
还有一些特殊用途的Hooks。useDebugValue用于在自定义Hook中添加DevTools显示标签，方便调试。useId用于生成唯一ID，主要服务于无障碍API。useSyncExternalStore订阅外部store实现跨框架状态共享。useActionState和useOptimistic是较新的Hooks，用于管理操作状态和乐观更新。这些Hooks主要供库作者使用。

14
00:07:00,000 --> 00:07:50,000
自定义Hooks是React最强大的特性之一。通过创建以use开头的JavaScript函数，我们可以把组件逻辑提取出来实现复用。比如useLocalStorage自动同步状态到本地存储，useFetch封装数据请求逻辑，useDebounce防抖处理用户输入。这些自定义Hooks让我们能把复杂的逻辑抽象成可复用的单元。

15
00:07:50,000 --> 00:08:05,000
今天的教程就到这里。我们学习了React提供的各种Hooks，包括State Hooks管理组件状态，Context Hook实现跨组件数据传递，Ref Hooks访问DOM节点，Effect Hooks处理副作用，Performance Hooks优化渲染性能，以及如何创建自定义Hooks来复用逻辑。掌握这些Hooks，你就能用函数组件构建出功能完整的React应用。感谢观看！
